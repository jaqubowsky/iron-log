# Sesja 2026-03-26

## Recall challenge

Pytanie: "ON DELETE CASCADE vs SET NULL vs RESTRICT — co się stanie, kiedy który?" (temat z sesji 2026-03-22, mock interview score 3/5). Score: 3.5/5 — Mechanika wszystkich trzech poprawna. Wybór RESTRICT z uzasadnieniem biznesowym (nie chcemy magicznie tracić ćwiczeń z szablonów) — trafny. Braki: nie podał scenariusza kiedy SET NULL ma sens (zamówienia po usunięciu usera), nie wspomniał o obsłudze błędu po stronie API (409 Conflict).

## Mini-recall

- "401 vs 403 — kiedy który?" — odpowiedział poprawnie: 403 forbidden (zalogowany, brak dostępu), 401 unauthorized (niezalogowany).

## Co robił

1. WorkoutTemplates repository — abstract class + PrismaWorkoutRepository + DI wiring z useClass
2. Aplikacyjny typ WorkoutTemplateDTO bez Prisma typów
3. Wydzielenie paginacji (offset + cursor) do src/common/ — reużywalne utility functions
4. 3 rundy code review z iteracyjnymi poprawkami architektonicznymi:
   - Runda 1: repository pattern OK, flagged DEFAULT_LIMIT duplikacja i findNonExistingExerciseIds cross-aggregate
   - Runda 2: znalezione krytyczne problemy — abstract class contract mismatch (limit vs take), repository importuje HTTP DTO, take getter w DTO ukrywa strategię fetchowania
   - Runda 3: wszystko poprawione — jawne mapowanie DTO → plain values w service, uczciwy kontrakt abstract class, getTake usunięty na rzecz inline limit + 1

## Planowanie architektoniczne

Pominięte — task był kontynuacją z poprzedniej sesji (znany pattern). Jakub znał kierunek, nie wymagał planowania. Przy pytaniach o szczegółowe pliki/kolejność słusznie odpowiedział że to prosty problem. Coach dostosował się (feedback zapisany: nie over-questionuj na prostych taskach).

## Code review

3 rundy. Kluczowe:
- **Runda 1:** Jakub sam zauważył boilerplate i odizolowanie od Prisma jako trade-off. Nie zauważył: DEFAULT_LIMIT duplikacji, findNonExistingExerciseIds w złym repo
- **Runda 2 (po poprawkach paginacji):** Agenty znalazły krytyczne issues — abstract class contract kłamie (limit vs take), repository importuje HTTP DTO (CursorPaginationDTO z class-validator decoratorami), take getter w DTO ukrywa limit+1 trick. Jakub poprosił o bardziej krytyczne review — trafna uwaga, coach był zbyt łagodny wcześniej
- **Runda 3 (po architektonicznych poprawkach):** Czyste — jawne mapowanie, uczciwy kontrakt, inline limit+1. Exercises service też poprawiony (structural matching → explicit mapping)

## Samodzielność (1-5)

4 — Najwyższa dotąd. Repository pattern napisany sam bez naprowadzania (znany pattern z ExercisesModule). Paginacja wydzielona samodzielnie z dobrą intuicją (zauważył niespójne API, sam poprawił getOffsetPagination). Poprawki architektoniczne z code review zrobione szybko i poprawnie. Potrzebował naprowadzenia na: separation of concerns (limit+1 w repository vs service), structural matching problem, i jawne mapowanie DTO → plain values. Sam zidentyfikował że getTake jest over-engineered. Sam wymusił bardziej krytyczne code review od coacha.

## Explain phase

(3.5/5) — Pokrył kluczowe decyzje: abstract class bez Prisma typów (wymienność implementacji), paginacja do common (DRY), logika paginacji w service nie w repo. Braki: nie potrafił wyjaśnić DLACZEGO limit+1 w repository to był problem (strategia paginacji vs query do bazy), pominął temat structural matching / explicit mapping.

## Mock interview

Pytanie: "WorkoutsService musi sprawdzić czy ćwiczenia istnieją — jakie opcje architektoniczne i trade-offy?" Score: 3/5. Trafnie: import ExercisesModule, YAGNI argument (nie wydzielaj na siłę), świadomość problemu z obecnym rozwiązaniem. Pominął: inne opcje (FK constraint, shared validation service, event-based), nie opisał mechanizmu (exports/imports w NestJS), nie wspomniał circular dependency risk.

## Co poszło dobrze

- Repository pattern end-to-end samodzielnie — bez ghostwritingu, szybko
- Sam zauważył niespójne API paginacji i sam poprawił (getOffsetPagination przebudowa)
- 3 rundy code review z iteracyjnymi poprawkami — za każdym razem lepiej, świadomy progres
- Sam wymusił bardziej krytyczne review od coacha — dojrzałe podejście do feedbacku
- Sam zidentyfikował że getTake to over-engineering — dobra intuicja
- Poprawki structural matching → explicit mapping zrobione szybko po wyjaśnieniu problemu
- Trafna korekta procesu coachingu: nie over-questionuj na prostych taskach + bądź bardziej krytyczny

## Co poszło źle

- Czas na dyskusje o agentach/skillach/procesie kosztem kodowania (~15 min)
- Nie potrafił wyjaśnić separation of concerns na głębszym poziomie (dlaczego, nie tylko że)
- Mock interview — jedna opcja zamiast 2-3, brak mechanizmu (exports/imports)
- Pierwsza wersja paginacji miała słabą architekturę (DTO z getterami, structural matching) — coach powinien był to wskazać wcześniej

## Słabości — update

- **Separation of concerns — głębokość** — nowa obserwacja. Wie ŻE trzeba separować, ale nie potrafi jeszcze wyjaśnić DLACZEGO konkretna separacja jest lepsza. Na rozmowie "bo tak się robi" to słaba odpowiedź
- **Mock interview — liczba opcji** — nadal podaje jedną opcję zamiast 2-3. Rekruter oczekuje alternatyw z trade-offami
- **Prokrastynacja przez eksplorację** — zmieniona forma: dyskusje o toolingu/agentach zamiast kodowania. Mniejsza niż poprzednio
- **SQL fundamenty** — nie ćwiczone dziś. CREATE TABLE, ACID recall nadal nieodhaczone
- **TS structural typing** — nowa wiedza: rozumie teraz getter vs plain object, structural matching jako fałszywe bezpieczeństwo. Wymaga utrwalenia

## Faza coachingu

Faza 1 (milestones 1-3). Samodzielność 4 — najwyższa dotąd. Znany pattern (repository) zrobiony szybko, nowy problem (paginacja extraction) rozwiązany samodzielnie z dobrą intuicją. Iteracyjne poprawki architektoniczne po code review — szybko i poprawnie. Progres widoczny vs poprzednie sesje. Zostajemy w fazie 1 ale blisko przejścia do fazy 2. Milestone 3.

## Następna sesja

**Kodowanie (główny task):** Cross-module communication — WorkoutLogsModule. Stwórz moduł do logowania treningów (WorkoutLog = "wykonałem ten szablon, te ćwiczenia, te serie"). WorkoutLogsService potrzebuje zweryfikować że WorkoutTemplate istnieje — zaimplementuj cross-module import/export. Przenieś `findNonExistingExerciseIds` do ExercisesModule (exports/imports). (M3 checkpointy: cross-module communication, zero circular deps)

**Retencja do wplenienia:** M2 SQL — ACID (każda litera z przykładem), constraints (ON DELETE — poprawia się, 3.5/5), EXPLAIN ANALYZE. M3 — idempotentność HTTP metod, REST vs GraphQL. Testuj przez recall challenge i mock interview.

**Nieprzerobione tematy do wplenienia:** M2: normalizacja (1NF-3NF), isolation levels, optimistic vs pessimistic locking. M3: HTTP caching (Cache-Control, ETag), N+1 problem, "co się dzieje gdy wpiszesz URL w przeglądarkę". Wpleć gdy pasuje do kontekstu — np. normalizacja przy projektowaniu WorkoutLog tabeli, N+1 przy omawianiu include/join w Prisma.

**Docs do przeczytania:** NestJS docs → Modules (sekcja o exports/imports, shared modules). Prisma docs → relations (one-to-many dla WorkoutLog → WorkoutTemplate).

**Nad czym pomyśleć:** WorkoutLog ma relację do WorkoutTemplate i do User (przyszły auth). Jak zaprojektujesz schemat bazy? Co jeśli user usunie szablon — co z logami treningów? (hint: to jest ten SET NULL case z recall challenge)
