# Sesja 2026-04-14

## Plan sesji
- **Czas:** 1h (w praktyce ~2h — przeciągnięte o debugging techdebt workouts + dwa nieudane próby fix field rename)
- **Milestone:** M4 — Auth + Security 🔴 BLOKUJE
- **Main task:** `POST /auth/register` — endpoint end-to-end
- **Rozgrzewka:** brak
- **Docs:** NestJS Security → Authentication; bcrypt npm README

## Task briefing topics

- topic: "bcrypt vs SHA256 — salt, cost factor, dlaczego do haseł" | milestone: M4 | status: briefed | keywords: [bcrypt, hash, salt, compare]

## Briefing utrwalenie check

- **OK: bcrypt vs SHA256** — anchor `src/auth/auth.service.ts:11` (`bcrypt.hash(data.password, 10)`). Briefing z tej sesji utrwalony w kodzie. Bank zaktualizowany — L3 anchor z `unknown` na konkretną ścieżkę.

## Co robił

Zaimplementował `POST /auth/register` end-to-end: `RegisterUserDTO` (boundary z class-validator), `CreateUserInput` (plain interface jako wewnętrzny kontrakt), `AuthService.register()` (bcrypt cost 10 → UsersService.create), `PrismaUsersRepository.create()` z `omit: { password: true }` + mapper, `PrismaUsersMapper.toUser` + `toUserWithPassword` przygotowany pod login. Globalny `PrismaExceptionFilter` ma łapać P2002 → 409 (runtime unverified bo build broken w workouts).

Dopisał `http/auth.http` z 11 scenariuszami testowymi (happy, duplikat, 9 edge cases walidacji, mass assignment probe).

## Planowanie architektoniczne

Plan solidny z twardego hypothesis-first flow:
- AuthService → UsersService → repo (layering czyste)
- Constraint catch zamiast pre-check — świadoma decyzja po dopytaniu o timing attack + race condition
- Response bez hasła → osobny `UserResponseDto` zamysł, faktycznie zrealizowany przez type separation app/repo + Prisma omit (senior pattern)
- Kolejność plików: DTO → service → controller → curl (bottom-up)

Ale: **slip ustaleń** — po planowaniu w trakcie implementacji dodał pre-check (`if (!user) throw ConflictException`) odwrócony logicznie. Naprawił po dopytaniu. Potem drugi slip: rename `password → hashedPassword` w DTO z `@IsHash('sha256')` (bcrypt to nie sha256) + field mismatch z Prisma schema. Oba naprawione po feedbacku. Trzeci iteration wyszedł clean — `CreateUserInput` jako plain interface z `hashedPassword` field, mapping `hashedPassword → password` w repo na boundary Prisma.

## Code review

Dwóch agentów w tle + sokratejski review Jakuba.

**Jakub sam zauważył:**
- bcrypt cost factor do shared config (M4 checkpoint follow-up)
- Kod podobał mu się — ale to wymagało ostrej konfrontacji z obroną `UsersService` jako "enkapsulacja biznesowej logiki" (która nie istnieje — Jakub przyznał że to aggregation point na przyszłość)

**Agent #1 (bugs/security) CRITICAL:**
1. `src/main.ts:8` — `ValidationPipe` nie ma `forbidNonWhitelisted: true` → mass assignment gap (extra pola po cichu strippowane)
2. `src/main.ts:9` + `src/app.module.ts:26` — `PrismaExceptionFilter` zarejestrowany dwukrotnie → drugi run próbuje pisać do zamkniętego socketu → potencjalny crash "headers already sent"

**Agent #2 (senior architectural):** Conditional Pass.
- Strong: constraint catch + omit, repository boundary, dwa-poziomowy kontrakt DTO/interface, `UserWithPassword` foresight
- Weak: brak explicit return types w public API, response shape register undefined (decyzja przed login), dead code `toUserWithPassword` bez callsite
- Cargo cult check: `toUserWithPassword` mapper istnieje bez callsite — Jakub obronił (pod login flow), to jest foresight nie cargo cult

**Drobne z review:**
- `bcrypt` cost `10` hardcoded → do ConfigService
- `P2002` message leak'uje field name → user enumeration
- `private userService` vs klasa `UsersService` — literówka
- `satisfies User` vs `: UserWithPassword` w mapperze — niespójność stylu

## Samodzielność (1-5)

**3** — Core implementacja samodzielna, plan architektoniczny solidny. Ale:
- 2 slipy w tej samej sesji (pre-check, field rename z @IsHash) — "self-review nawyk" (słabość z 2026-04-04) nadal aktywna
- UsersService obroniony pustym słowem ("enkapsulacja") — cargo cult risk w słownictwie
- Nie zobaczył 2 CRITICAL bugów (ValidationPipe + double filter) — agent musiał pokazać

Progres vs 2026-04-04 (score 3): stabilne 3, ale z różnym profilem słabości. Decyzje architektoniczne mocniejsze, slipy implementacyjne słabsze.

## Explain phase (Feynman)

**3/5** — punkt 1 (DTO boundary vs plain interface) solidny 3.5/5, ale z żargonem bez definicji ("endpoint", "walidatory", "plain interface"). Punkt 2 (constraint catch) trafił timing attack samodzielnie, ale pominął race condition — wyciągnięte dopytaniem. Junior bez backgroundu nie załapałby tłumaczenia.

Dobra rzecz: **TOCTOU concept pojawił się dopiero jako mój feedback** — Jakub go nie znał, ale zrozumiał natychmiast gdy nazwany.

## Articulation check

- **FK constraints CASCADE/SET NULL/NO ACTION/RESTRICT** (M2): 3.5/5 (poprzednio 3.0) — interval 3d → 46d | anchor: `prisma/schema.prisma:87`
  - Trafne: 4 scenariusze z IRONLOG (CASCADE cwiczenia+serie, RESTRICT user z zamówieniami, SET NULL snapshot, SET DEFAULT kategoria→general)
  - Po dopytaniu: NO ACTION vs RESTRICT różnica (deferred vs immediate check) precyzyjnie
  - Nowe "Do domknięcia": NO ACTION jako Postgres default, `DEFERRABLE INITIALLY DEFERRED` use case
  - **Progres 3.0 → 3.5 po 23 dniach** — wymienił samodzielnie SET DEFAULT którego nie znał w marcu
- **Offset vs cursor pagination** (M3): 3.5/5 (poprzednio 3.0) — interval 3d → 40d | anchor: `src/workouts-logs/workouts-logs.controller.ts:1`
  - Trafne: duplikat scenariusz (new insert → push → duplicate page 2), mechanizm LIMIT/OFFSET vs WHERE+index, OFFSET = read everything
  - Near miss: "UUID cursor zbugowane" (faktycznie przechodzi, tylko semantycznie bez sensu — random UUID nie daje ciągłości)
  - Nowe "Do domknięcia": composite cursor `(createdAt, id)`, B-tree index seek nazwany, MVCC visibility nuance
  - **Progres 3.0 → 3.5 po 20 dniach** — pojawił się WHERE mechanism + index reasoning, których nie było w marcu

**Lapse notatki:** brak
**Bridge needed:** brak (oba tematy mają anchory w kodzie)

## Nowe L2 topics (dodane mid-session)

Brak nowych L2 topics dodanych w trakcie. **Ale rekomendacja do dodania przed następną sesją** (concept gaps z code review):

1. **"Constraint catch vs pre-check — race condition (TOCTOU) + timing attack"** (M4) — Jakub podjął decyzję ale dwa razy dzisiaj slipował w stronę pre-checka. Temat wymaga solidnej artykulacji żeby decyzja wiedziała w mięśniach, nie tylko w głowie
2. **"Validators at boundary vs plain interface inside — DTO vs internal contract"** (M4) — świadoma decyzja podjęta dzisiaj w oparciu o feedback, warta utrwalenia jako narracyjny topic

(Oba do dodania przez session-end jako explicit topic add — format score 2.5/5 explain tag, interval 3d. Ale to wymagałoby kolejnego Edit banku — zostawiam dla następnego session-end lub ad-hoc articulation-check).

## Co poszło dobrze

- **Constraint catch + Prisma omit + mapper** — senior-level decision, defense in depth (level 1 + level 2 razem)
- **`CreateUserInput` jako plain interface** — świadomy refactor po lekcji "validators belong at boundaries". Dokładnie tym, co senior by zrobił
- **TOCTOU + race condition zrozumiany po jednym pytaniu** — intuicja bezpieczeństwa działa
- **Dependency chain thinking** — Jakub sam zidentyfikował że workouts techdebt wymaga całego JWT flow + `@CurrentUser()` zanim się naprawi. Working backwards from consumer to root. Senior thinking.
- **FK constraints recall 3.0 → 3.5** — wymienił 4 scenariuszy IRONLOG konkretnie, z SET DEFAULT który był luką w marcu
- **Offset vs cursor recall 3.0 → 3.5** — mechanizm WHERE + index się pojawił

## Co poszło źle

- **2 slipy w tej samej sesji** — pre-check odwrócony logicznie + `@IsHash('sha256')` na bcrypt hash. Oba pokazują że Jakub klepie kod bez mentalnego odpalenia
- **UsersService broniony słowem bez treści** — "enkapsulacja biznesowej logiki" której nie ma. Cargo cult w słownictwie (wie jak mówić, nie wie *dlaczego*)
- **2 CRITICAL bugi niezauważone** — ValidationPipe whitelist + double filter registration. Jakub sprawdzał swój kod ale nie środowisko runtime (main.ts + app.module.ts)
- **Ghostwriting attempt**: prośba o boilerplate AuthRepository gdy go nie potrzebował (sam się wycofał po dopytaniu — ale to był test)
- **Workouts techdebt odkryty po 9 dniach** — nie uruchamiał pełnego buildu od 2026-04-04. Lekcja operacyjna: build na końcu każdej sesji

## Słabości — update

- **Self-review nawyk** — NADAL. 2 slipy w sesji + 2 CRITICAL niezauważone. Słabość aktywna od 2026-04-04 (3 sesje temu, brak poprawy). **Akcja:** session-start musi wymusić `pnpm build && pnpm lint` jako pre-check przed uznaniem taska za gotowy, nie tylko Jakub mówi "gotowe"
- **Żargon bez definicji** — NOWA obserwacja z explain phase. Jakub zna terminy ale nie potrafi ich rozłożyć na plain language. Na rozmowie z nietechnicznym PM byłby problem. **Akcja:** explain phase następne 3 sesje ma tylko plain-language format, bez "hacków" do technicznego języka
- **Zapominanie decyzji mid-implementation** — pre-check wylazł dwa razy. **Akcja:** przy wyborze architektonicznym Jakub ma to zapisać w komentarzu w kodzie ("// REASON: constraint catch, not pre-check — race + timing") — samo zapisanie buduje pamięć motoryczną

## Faza coachingu

**Faza 1** (M1-M3 core zakończone, M4 aktywny). Samodzielność 3 — stabilna od 2026-04-04, zero progresu do Fazy 2. Przejście wymaga 4+ sesji z oceną 4-5. Dzisiejsze 3 (2 slipy + 2 CRITICAL niezauważone) nie liczy się jako gotowość.

**Pierwsza sesja kodowa w M4 zaliczona.** JWT flow mental model działa (4/5 na 2026-04-13). Następny krok: login + JWT strategies.

## Następna sesja

### Rekomendacja głównego taska

**Priority 1: Fixy po review + runtime weryfikacja register (~20 min rozgrzewki):**
1. `ValidationPipe` → `forbidNonWhitelisted: true` (main.ts)
2. Usunąć double registration `PrismaExceptionFilter` (zostaw APP_FILTER w app.module, usuń useGlobalFilters w main.ts, użyj typed `APP_FILTER` z `@nestjs/core`)
3. Explicit return types: `AuthService.register(): Promise<User>`, `UsersService.create(): Promise<User>`, `UsersService.findByEmail(): Promise<User | null>`, `AuthController.register(): Promise<User>`
4. Po fixach: uruchomić `pnpm start:dev` (jeśli workouts build nadal broken, użyć `pnpm exec tsc --noEmit` na konkretnych plikach) + odpalić `http/auth.http` requesty #1-#4. **Must-verify:** request #2 (duplikat) zwraca 409, nie 500

**Priority 2 (główny task):** `POST /auth/login` + `LocalStrategy`
- `UsersRepository.findByEmailWithPassword()` — dedykowany method (mapper `toUserWithPassword` dostaje callsite — dead code się aktywuje)
- `LocalStrategy` implementowana — `validate(email, password)` sprawdza bcrypt.compare, zwraca user lub rzuca UnauthorizedException
- `AuthGuard('local')` na `POST /auth/login`
- Wystawianie access token (JwtModule.register, signOptions) + RefreshToken persist do DB

**Decyzja którą Jakub ma podjąć PRZED implementacją:** response shape register — auto-login (zwraca tokeny) czy `{id, email}` i oddzielny login? Do zdecydowania w Priority 2 planning stage.

**NIE ROBIMY jeszcze:** fix workouts techdebt (to jest Priority 3 na sesję N+3 gdy będzie `@CurrentUser()` decorator)

### Task briefing — relevant score-0 topics (M4)

1. **Passport strategy lifecycle — jak `validate()` jest wywoływana, co zwraca, skąd AuthGuard bierze user** (M4, score 0) → **CRITICAL briefing** — login flow startuje od LocalStrategy, Jakub nigdy nie widział signpostingu
2. **JWT vs session-based auth — trade-offy** (M4, score 0) → secondary, ale login flow decision o wystawianiu tokena naturalnie prowokuje ten temat
3. **Config management — env vars, secrets, ConfigModule między środowiskami** (M4, score 0) → trzeci priority, nadaje się bo bcrypt cost + JWT_SECRET idą do env vars w tej samej sesji

Hard limit 1h sesji = max 15 min briefing, max 4 topics. Wszystkie 3 powyżej są relevant i core M4 — lecą w briefingu.

### Obserwacje z sesji (input dla session-start)

- **Słabości do zaadresowania:**
  - Self-review nawyk: wymusić `pnpm build` + odpalenie start:dev + manual curl PRZED uznaniem taska za gotowy
  - Żargon bez definicji: explain phase w plain language only
  - Zapominanie decyzji: komentarz "// REASON: X" w kodzie przy każdej architektonicznej decyzji
- **Poprawki z review (rozgrzewka priority 1):** 4 fixy opisane wyżej — ~20 min
- **Co poszło dobrze:** constraint catch senior pattern, CreateUserInput boundary decyzja, dependency chain thinking, FK + cursor recall progres 3.0 → 3.5

### Materiały do nauki przed sesją

- **Docs:** NestJS docs → Security → Authentication → "Implementing Passport local" (dokładna sekcja dla LocalStrategy). Pobrać przez context7 przed sesją
- **Pytanie do przemyślenia:** Login endpoint — co zwracasz gdy email istnieje ale hasło złe vs gdy email nie istnieje? Ten sam 401, czy różne? Jakie są konsekwencje każdego wyboru (timing attack, user enumeration, UX)? Pomyśl o tym jak o register constraint catch — ten sam pattern myślenia o boundary'ach bezpieczeństwa
- **Warunek wstępny:** przed siadaniem do login, uruchom `pnpm start:dev` i sprawdź czy register faktycznie działa runtime-em. Jeśli nie — priority 1 fixy są BLOCKEREM, nie rozgrzewką
