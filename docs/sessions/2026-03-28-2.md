# Sesja 2 — 2026-03-28

## Plan sesji
- **Czas:** 1h
- **Milestone:** M3 (🔴 BLOKUJE — HTTP/REST teoria)
- **Rozgrzewka:** brak
- **Main task:** List/detail separation w workout-logs — lekki preview na findMany, pełny detail na findOne
- **Temat poboczny:** PrismaWorkoutLogMapper — mapowanie Prisma→typy aplikacyjne w repo
- **Docs:** Prisma docs → select, _count

## Recall challenge

Pytanie: "NestJS cross-module communication — opcje architektoniczne, mechanizm exports/imports, trade-offy" (mock interview 2026-03-26, 3/5). Score: 3.5/5 — DI (import/export) opisany poprawnie z krokami (eksportuj serwis, importuj moduł, wstrzyknij w konstruktorze). Event-based jako alternatywa z trafnym trade-offem (overengineering dla prostych przypadków). Moment "aha": moduł to konfiguracja/pudełko, providery (serwisy) są singletonami — wcześniej myślał że moduł jest singletonem. FK constraint słusznie odrzucony jako nie-komunikacja. Progres vs 3/5 z 2026-03-26.

## Co robił

1. List/detail separation w workout-logs — `findMany` zwraca lekki preview (id, name, description, dates, exercises z category i setsCount), `findOne` zwraca pełny detail
2. Prisma `select` z `_count` w repo — ściąga tylko potrzebne pola z bazy
3. `PrismaWorkoutLogMapper` — nowa klasa z `Prisma.GetPayload<>` typami, mapowanie Prisma→typy aplikacyjne we wszystkich metodach repo (nie tylko findMany)
4. `WorkoutLogSimpleResponseDTO` — nowe response DTO dla listy z `.from()` static method
5. Interfejs `WorkoutLogSimple` — aplikacyjny typ dla preview
6. Abstract `WorkoutLogRepository` zaktualizowany — `findMany` zwraca `WorkoutLogSimple[]`
7. Poprawki z code review: spread bug (workoutLogExercises + exercises), exercises pass-through w mapper (jawne mapowanie pole po polu)

## Planowanie architektoniczne

Kierunkowo poprawny — wiedział że trzeba ruszyć DTO, repo, service. Dużo wahania przy wyborze podejścia: raw SQL → Prisma _count → raw SQL → Prisma _count z reduce. Sam doszedł że raw SQL to overkill na jeden reduce (po naprowadzeniu pytaniem "czy sumowanie tablicy to logika biznesowa?"). Nie wymienił konkretnych plików/kolejności — odmówił ("oszalałeś?") co jest OK na znanym patternie. Dobra decyzja biznesowa o kształcie preview (breakdown per kategoria zamiast flat count) — uzasadniona potrzebą frontend.

## Code review

2 rundy agentów + sokratejska dyskusja. Kluczowe:
- **Jakub sam zauważył:** niespójność między modułami (brak PrismaMapper w exercises/workouts), "abstrakcyjne repo boli w utrzymaniu" — trafna obserwacja z uzasadnieniem (manualne typy)
- **Agenty flagged:** exercises pass-through w `toWorkoutLog` (Prisma internals przeciekają), brak `orderBy` na workoutLogExercises, niespójność DELETE response (pełny obiekt vs string message), templateId w interfejsie ale nie w response DTO
- **Naprawione na sesji:** exercises pass-through → jawne mapowanie, spread bug

## Samodzielność (1-5)

3 — List/detail separation zrobiony, ale dużo wahania przy wyborze podejścia (raw SQL vs _count — 3 zmiany kierunku zanim się zdecydował). PrismaWorkoutLogMapper sam zaproponował — dojrzała decyzja. Prisma _count syntax wymagał naprowadzenia (select + include razem, _count: true vs select wewnątrz). Spread bug nie zauważony sam. Kształt preview (per kategoria) — dobra samodzielna decyzja biznesowa.

## Explain phase

(3.5/5) — Wymienił dwa kluczowe elementy (mapper Prisma→typy aplikacyjne, lekki findMany). Uzasadnienia: select na poziomie bazy (performance), mapper w repo (service nie powinien wiedzieć o Prisma), tablica per kategoria (potrzeba frontend). Braki: przy pytaniu "ORM to już abstrakcja — po co repo nad ORMem?" nie umiał odpowiedzieć sam — potrzebował naprowadzenia na testowalność. Trade-offy preview kształtu jednostronne (co zyskuje, nie co traci).

## Mock interview

Pytanie: "Po co repository pattern w backendzie? Kiedy dobre rozwiązanie, a kiedy overengineering?" Score: 3/5. Trafnie: separacja szczegółów bazy, service nie musi wiedzieć skąd dane, overengineering = abstrakcyjne repo w prostych appkach. Pominął: testowalność (główny praktyczny argument — mockowanie repo), "ORM to już abstrakcja" (nie potrafił odpowiedzieć na follow-up), wymiana bazy jako rzadki rzeczywisty use case. Po follow-up uzupełnił testowalność i wymienność bazy. Odpowiedź jednostronna — koszty podał, zyski dopiero po dopytaniu.

## Co poszło dobrze

- PrismaWorkoutLogMapper sam zaproponował i uzasadnił architektonicznie — dojrzałe myślenie o separacji warstw
- Kształt preview (per kategoria z setsCount) — samodzielna decyzja biznesowa z uzasadnieniem
- Sam doszedł że raw SQL to overkill na jeden reduce — dobry instynkt po wahaniu
- Poprawki z code review zrobione szybko i poprawnie
- "Abstrakcyjne repo boli w utrzymaniu" — trafna obserwacja z doświadczenia, nie z teorii
- Recall: moment "aha" o modułach vs providerach jako singletonach — głębokie zrozumienie

## Co poszło źle

- Dużo wahania przy wyborze podejścia (raw SQL vs _count — 3 zmiany kierunku) — marnuje czas
- Prisma _count syntax — nie znał, próbował select + include razem, _count: true zamiast select
- Spread bug (workoutLogExercises + exercises w obiekcie) — nie zauważył sam
- Mock interview — jednostronna odpowiedź (koszty bez zysków), "niemożliwe" zamiast "trudniejsze" przy testowaniu bez repo
- "ORM to już abstrakcja" — nie umiał odpowiedzieć na to follow-up sam

## Słabości — update

- **Trade-offy — obie strony** — NADAL. Na explain i mock daje jedną stronę (koszty LUB zyski), drugą dopiero po dopytaniu. Pattern z poprzednich sesji (REST vs GraphQL — argument popularności zamiast technicznych). Na rozmowie rekruter zawsze dopyta o drugą stronę
- **Wahanie przy wyborze podejścia** — NOWA obserwacja. Raw SQL → _count → raw SQL → _count — 3 zmiany w 10 min. Wynika z braku pewności w trade-offach (nie umie szybko ocenić co lepsze). Rozwiąże się z doświadczeniem ale warto świadomie ćwiczyć: "jakie 2 opcje? trade-offy? decyzja."
- **Prisma API knowledge** — select vs include, _count syntax — nie znał. Normalne na tym etapie, rozwiąże się z praktyką + docs

## Faza coachingu

Faza 1 (milestones 1-3). Samodzielność 3 — niżej niż 4 z sesji 2026-03-26, ale inny charakter (nowy pattern vs powtórzenie znanego). PrismaMapper sam zaproponował — sygnał dojrzałości. Zostajemy w fazie 1. Milestone 3.

## TODO na przyszłe sesje
- Ujednolicić pattern list/detail separation w exercises i workout-templates (PrismaMapper, select w findMany, response DTO preview)
- Rozważyć PrismaMapper w exercises i workouts repo (spójność z workout-logs)
- Dodać orderBy na workoutLogExercises w obu query
- Ujednolicić DELETE response convention między modułami

## Następna sesja

### Rekomendacja głównego taska
Kontynuacja M3 HTTP/REST teoria — zostały 3 checkpointy 🔴 zero: API versioning (URL vs header), Cache-Control headers, ETag vs Last-Modified. Jakub musi przeczytać materiały PRZED sesją (MDN Cache-Control, ETag, API versioning). Alternatywa kodowa: ujednolicenie PrismaMapper + list/detail separation w exercises i workout-templates (spójność między modułami). Uzasadnienie: M3 teoria blokuje milestone, ale Jakub wyraźnie preferuje kodowanie — alternatywa kodowa daje wartość i ćwiczy znany pattern w nowym kontekście.

### Obserwacje z sesji (input dla session-start)
- **Słabości do zaadresowania:** Trade-offy jednostronne (koszty bez zysków), wahanie przy wyborze podejścia (3 zmiany kierunku), "ORM to już abstrakcja" — nie umiał odpowiedzieć
- **Poprawki z review:** orderBy na workoutLogExercises (nie bloker), DELETE response niespójność (nie bloker), templateId usunięty z response DTO (świadoma decyzja)
- **Co poszło dobrze:** PrismaMapper sam zaproponował, kształt preview samodzielna decyzja, recall moment "aha" o modułach

### Retencja (tematy do weryfikacji)
- Repository pattern — kiedy overengineering, ORM jako abstrakcja (mock 2026-03-28, 3/5)
- NestJS modules — moduł to konfiguracja, providery to singletony (recall 2026-03-28, 3.5/5 — nowe zrozumienie)
- Normalizacja 1NF/2NF/3NF (⏳ 3.5/5 — 2026-03-27, nie testowane od 1 sesji)

### Materiały do nauki przed sesją
- **Docs:** MDN → Cache-Control header (max-age, no-cache, no-store, stale-while-revalidate). MDN → ETag, Last-Modified, conditional requests. Dowolny artykuł o API versioning strategies (URL vs header vs query param)
- **Pytanie do przemyślenia:** GET /exercises zwraca 500 ćwiczeń które zmieniają się raz na miesiąc. Jaki Cache-Control header ustawiasz? A co gdy user dodał nowe ćwiczenie — jak invalidujesz cache?
