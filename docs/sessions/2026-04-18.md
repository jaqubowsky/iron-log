# Sesja 2026-04-18

## Plan sesji
- **Czas:** 1h
- **Milestone:** M4 — Auth + Security 🔴 BLOKUJE
- **Main task:** Fix bugów w `AuthService.login` (rozgrzewka) → runtime verify login endpoint → logout endpoint
- **Rozgrzewka:** 4 fixy w AuthService.login z review 2026-04-17 (refresh token JWT → opaque UUID, persist do DB, env var REFRESH_EXPIRES_IN_MS)
- **Docs:** NestJS Security → Guards (jeśli zahaczysz o metadata-driven decisions przy logout)

## Task briefing topics

- topic: "Guard vs Middleware — oba blokują request, kiedy który" | milestone: M4 | status: briefed | keywords: [guard, middleware, canactivate, executioncontext, reflector, usegards]

## Co robił

Fix bugów login endpoint (opaque refresh token, persist do DB, env var, APP_GUARD przeniesiony do AppModule, Zod passthrough), debugowanie 500 (Zod strippował DATABASE_URL), runtime verify login flow przez HTTP client. Dodano `createStrategyUserDecorator<T>()` factory, `@Public()` decorator + `JwtAuthGuard` z Reflector, global APP_GUARD. Naprawiono workouts TypeScript errors (userId w repo, nullable ?? null).

## Planowanie architektoniczne

Wariant (b) dla techdebt — @CurrentUser + workouts fix jako pre-req do runtime verify. Cookie w kontrolerze (nie serwisie) — Jakub sam zidentyfikował separation of concerns. APP_GUARD placement — po naprowadzeniu przeniósł do AppModule z prawidłowym uzasadnieniem.

## Samodzielność (1-5)

**4** — `createStrategyUserDecorator<T>()` generic factory samodzielnie (niespodziewane), cookie w kontrolerze intuicyjnie, APP_GUARD po naprowadzeniu z rozumieniem uzasadnienia. Debugowanie Zod passthrough — coach pokazał mechanizm. httpOnly: true (nie tylko produkcja) — sam po pytaniu. Runtime verify przeprowadzony.

## Explain phase

**4/5** — refresh token hash mechanizm + weryfikacja przez ponowny hash samodzielnie. Brak salta jako klucz argument (UUID = wysoka entropia). Płynne, plain language.

## Notatki na bieżąco

- dobry moment: createStrategyUserDecorator<T>() — generic factory dla type-safe param decoratorów, samodzielnie
- dobry moment: APP_GUARD → AppModule po naprowadzeniu, rozumiał uzasadnienie
- dobry moment: cookie w kontrolerze (nie service) — separacja odpowiedzialności sam zidentyfikował intuicję
- słabość: REFRESH_EXPIRES_IN_MS z.string() vs z.coerce.number() — nie zauważył type mismatch przed review

## Articulation check

- **NestJS modules** (M3): 3.5/5 (poprzednio 3.0) — 4 opcje komunikacji samodzielnie, circular dependency mechanizm po dopytaniu, pominął rozwiązanie architekturalne i API call. Interval 3d → 46d.
- **Composite PK vs auto-increment** (M2): nie testowane — Jakub kończył sesję (4% baterii). Przeniesione na następną sesję.

## Briefing utrwalenie check

- **Guard vs Middleware** (briefed): PENDING — keywords [guard, middleware, canactivate, executioncontext, reflector] nie matchują się bezpośrednio w git diff (kod był z poprzedniej sesji, nie dzisiejszy diff). Score 1.5/5 — zostaje w rotacji, wraca jutro (2026-04-19).

## Odhaczone L3 (2026-04-18)

- [x] `AuthGuard('local')` — anchor `src/auth/auth.controller.ts:21`
- [x] `AuthGuard('jwt')` — APP_GUARD global, anchor `src/app.module.ts:40`
- [x] `POST /auth/login` — runtime verified, anchor `src/auth/auth.controller.ts:23`
- [x] `@CurrentUser()` decorator — anchor `src/auth/decorators/strategy-user.decorator.ts:1`
- [x] ConfigModule — anchor `src/app.module.ts:16`

## Co poszło dobrze

- `createStrategyUserDecorator<T>()` factory — niespodziewane, senior-level pattern
- Cookie w kontrolerze (nie serwisie) — sam zidentyfikował separation of concerns
- Runtime verify LOGIN przeprowadzony po raz pierwszy po 4 sesjach
- httpOnly: true po jednym pytaniu (nie tylko produkcja)
- NestJS modules articulation: 3.0 → 3.5 po 23 dniach

## Co poszło źle

- Zod `.passthrough()` — nie widział type mismatch między schema a `process.env.DATABASE_URL`. Coach musiał wskazać mechanizm.
- APP_GUARD placement — prawidłowe po naprowadzeniu, nie samodzielnie

## Słabości — update

- **Self-review nawyk** — ZAMKNIĘTE. Runtime verify przeprowadzony. Login działa end-to-end.
- **Slip ustaleń** — brak dziś (refresh token opaque przeszedł poprawnie).
- **Ownership na PATCH/DELETE** — NOWA, świadomie odłożona przez Jakuba. Dotyczy workouts i workout-logs.

## Faza coachingu

**Faza 1** (M4 aktywny). Samodzielność **4** — `createStrategyUserDecorator` factory + cookie placement + runtime verify bez slipu. Progres vs 2026-04-17 (3.5). Trzy sesje z rzędu: 3 → 3.5 → 4. Trend rosnący.

## Następna sesja

### Rekomendacja głównego taska

**Rozgrzewka:** Composite PK articulation check (2 min) — przeniesione z dzisiejszej sesji.

**Main task: `POST /auth/logout`** — M4 checkpoint. Wymaga: `JwtAuthGuard` na endpoint, hash incoming refresh cookie, `prisma.refreshToken.updateMany({where: {tokenHash, userId, valid: true}, data: {valid: false}})`, `res.clearCookie`, 204 response. Idempotentne (drugi call też 204). Potem: `JwtRefreshStrategy` jako prerequisite do `POST /auth/refresh`.

### Task briefing — relevant score-0 topics (M4)

- **OWASP top 3 (XSS, SQL injection, CSRF)** (M4, score 0) — relevant bo httpOnly cookie + SameSite jako CSRF mitigation
- **CORS** (M4, score 0) — relevant do security basics w M4

### Obserwacje z sesji

- **Poprawki z review:** drobne — AuhtRepository typo (4 pliki), LoginUserResponseDTO jako interface zamiast klasy — jako rozgrzewka 5 min
- **Ownership guard** — odłożony świadomie. Dopisać jako L3 checkpoint jeśli nie ma, lub oznaczyć jako priority po logout/refresh.
- **Co poszło dobrze:** createStrategyUserDecorator factory — podkreślić że to senior-level pattern. Runtime verify zamknięty.

## Code review — internal memo (session-end input)

**Mapping do nauki:**
- L3 ANCHOR: `POST /auth/login` — `src/auth/auth.controller.ts:24` (login endpoint z opaque refresh token + httpOnly cookie). Checkpoint odhaczony warunkowo — runtime verify nie przeprowadzony.
- L3 ANCHOR: `@CurrentUser()` custom decorator — `src/auth/decorators/strategy-user.decorator.ts:1` (createStrategyUserDecorator factory). Checkpoint zaliczony.
- BRIDGE NEEDED: Ownership guard — `PATCH /workout-templates/:id`, `DELETE /workout-templates/:id`, `PATCH /workout-logs/:id`, `DELETE /workout-logs/:id` nie sprawdzają userId. M4 checkpoint, Jakub świadomie odłożył. Wymaga dopisania `(bridge)` lub oznaczenia L3 jako blokującego.

**Pełna lista findings:**
- CRITICAL (fixed): REFRESH_EXPIRES_IN_MS z.string() → z.coerce.number() — string concatenation z Date.now()
- ARCHITECTURAL (fixed): APP_GUARD w AuthModule → przeniesiony do AppModule
- ARCHITECTURAL (fixed): import { Response } w auth.service — usunięty
- ARCHITECTURAL (open): brak ownership check na PATCH/DELETE workouts — odłożone świadomie przez Jakuba
- ARCHITECTURAL: workouts update używa batch $transaction zamiast interactive — brak rollback gwarancji przy exercise replacement
- MINOR: AuhtRepository typo (4 pliki)
- MINOR: LoginUserResponseDTO klasa bez dekoratorów — powinien być interface
- MINOR: sameSite: 'lax' bez path restriction na refresh cookie
