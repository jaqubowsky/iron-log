# Sesja 2026-04-27

## Plan sesji
- **Czas:** 2h
- **Milestone:** M4 — Auth + Security 🔴 BLOKUJE
- **Main task:** Rate limiting — global IP-based throttle + per-route limit na /auth/login i /auth/register (CORS + helmet już wdrożone przez Jakuba)
- **Rozgrzewka:** brak
- **Docs:** NestJS Security → CORS + Helmet + Throttler (@nestjs/throttler)

## Task briefing topics

brak

## Code review — internal memo (session-end input)

**Mapping do nauki:**
- L3 ANCHOR: Security basics (CORS + helmet + rate limiting) → `src/app.module.ts:35` + `src/auth/auth.controller.ts:48` — checkpoint gotowy do odhaczenia po poprawce trust proxy
- CONCEPT GAP: `trust proxy` w Express/NestJS — produkcyjna pułapka rate limitingu za reverse proxy; brak wpisu w banku, temat naturalny dla M7 (production readiness)

**Pełna lista findings (synthesis):**
- ARCHITECTURAL: `trust proxy` brak w `main.ts` — za reverse proxy wszystkie IP = proxy IP, rate limit bezużyteczny lub blokuje wszystkich naraz
- ARCHITECTURAL: `name: 'default'` brak w ThrottlerModule.forRoot — implicit coupling, delikatne przy dodaniu drugiego throttlera
- MINOR: `/logout` brak per-route limitu
- MINOR: `getHello()` scaffolding endpoint nadal istnieje (konsumuje throttle budget, zwraca 401)
- MINOR: `z.url()` blokuje multi-origin i wildcard — latent footgun
- MINOR: `THROTTLE_TIME_MS` zduplikowany w dwóch plikach
- MINOR: `process.env.NODE_ENV` bezpośrednio zamiast ConfigService

## Co robił

Rate limiting — `ThrottlerModule.forRoot` (global 50/min) + `ThrottlerGuard` jako APP_GUARD + `@Throttle` per-route na `/register`, `/login` (5/min), `/refresh` (20/min). Fix review: `name: 'default'` w ThrottlerModule + `trust proxy = 1` w main.ts z `NestExpressApplication` generic.

## Planowanie architektoniczne

Brak formalnego planowania — task był jasny (dokumentacja wystarczyła), zaimplementował samodzielnie.

## Code review

- Jakub sam: "git, ewentualnie ujednolicić stałe" — trafne ale powierzchowne
- Pominął: trust proxy (ARCHITECTURAL #1), implicit default throttler name (ARCHITECTURAL #2)
- Naprawił oba po naprowadzeniu + wyjaśnieniu trust proxy

## Samodzielność (1-5)

**4** — implementacja z dokumentacji samodzielna, oba ARCHITECTURAL po naprowadzeniu zrozumiał i naprawił. Faza 2 zachowanie.

## Explain phase

**4/5** — rate limiting (brute force + mechanizm zliczania) poprawnie. Trust proxy (bramkarz + ostatni wpis XFF) poprawnie po wcześniejszym explain-concept. Bez hintów.

## Articulation check

- **Event loop (M5):** 3.0/5 (bez zmian) — szkielet sync→micro→macro poprawny, nextTick>Promise.then po pytaniu, libuv bez nazwy. Interval 3d → 28d
- **Streams + backpressure (M5):** 3.5/5 (poprzednio 3.0) — memory pressure po korekcie, backpressure samodzielnie. Interval 3d → 46d

Lapse notatki: brak
Nowe słabości (Do domknięcia): libuv delegation (pojawia się po wyjaśnieniu), BullMQ + 202 Accepted (nigdy samodzielnie)

## Briefing utrwalenie check

N/A — brak briefingu dzisiaj.

## Odhaczone L3

- [x] Security basics: CORS + helmet + rate limiting — anchor `src/main.ts:10` + `src/app.module.ts:35` + `src/auth/auth.controller.ts:48`

## Co poszło dobrze

- Rate limiting z dokumentacji bez pomocy — typowe Faza 2
- Trust proxy konceptualnie trafny po explain-concept (bramkarz analogy)
- Backpressure samodzielnie, bez podpowiedzi
- Proaktywny skill fix (articulation-check re-recall + session-start codebase verification)

## Co poszło źle

- Trust proxy pominięty w self-review (niewidoczny bez znajomości production deployment)
- Event loop: libuv delegation nadal wymaga wyjaśnienia
- Streams: blocking vs memory pressure pomylone (szybko skorygowane)

## Słabości — update

- **libuv / I/O delegation** — NOWA. Konceptualnie rozumie "non-blocking", ale "dlaczego" bez wyjaśnienia nie pada
- **BullMQ + 202 Accepted** — NADAL. Nie pojawia się samodzielnie przy omawianiu async processing

## Faza coachingu

Faza 2 (M4-M6). Ostatni core `[ ]` w M4: unit testy AuthService.

## Następna sesja

### Rekomendacja głównego taska
`Min. 2 unit testy dla AuthService (validateUser, hashPassword)` — ostatni core M4 checkpoint. Zrealizowanie zamknie M4 i odblokowuje M5.

### Task briefing — relevant score-0 topics
Brak — żaden score-0 topic z M4 lub wcześniejszych nie dotyczy bezpośrednio unit testów. Tematy z M7 (`Test.createTestingModule`, `Unit vs integration vs e2e`) są gated (M7 > M4).

### Obserwacje z sesji (input dla session-start)
- **Słabości do zaadresowania:** libuv/I/O delegation (wraca w M5 stream endpoint)
- **Poprawki z review:** brak (wszystkie naprawione w sesji)
- **Co poszło dobrze:** rate limiting samodzielnie, trust proxy po explain-concept solidny, backpressure

### Materiały do nauki przed sesją
- **Docs:** NestJS → Testing → Unit testing (`@nestjs/testing`, `Test.createTestingModule`)
- **Pytanie do przemyślenia:** Jak zamockować zależność w unit teście — czym różni się `useValue` od `useClass` w TestingModule?

## Notatki na bieżąco
