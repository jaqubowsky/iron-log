# Sesja 2026-03-27

## Recall challenge

Pytanie: "SQL-level vs app-level data transformation — 3 trade-offy z przykładami" (mock interview 2026-03-23, 2.5/5 → recall 2026-03-24, 3.5/5). Score: 3.5/5 — stabilny. Wymienił: performance (nie ściągasz całej tabeli), reużywalność jako trade-off SQL-level (query per endpoint), app-level do ukrywania pól (DTO). Pominął: agregacje (baza liczy szybciej), testowalność (app-level łatwiej testować unit testem), złożona logika (formatowanie, lokalizacja czytelniejsza w app-level). Uwaga: recall w nowym formacie dialogowym — dopytanie było po ocenie, nie przed. Następnym razem dopytaj PRZED oceną.

## Rozgrzewka — batch vs interactive transaction

Jakub obronił batch transaction z konkretnymi argumentami: prostsze API, nie potrzebuje wyników między operacjami, rollback działa w obu. Sam sprawdził docs Prisma (context7) — potwierdził rollback. Poprzednia sesja oznaczyła to jako bloker zbyt pochopnie — NIE jest blokerem. Dobry moment: uzasadnił decyzję techniczną tak jak na rozmowie.

## Co robił

1. CREATE TABLE z pamięci — WorkoutLog, WorkoutLogExercise, WorkoutLogExerciseSet w raw SQL. FK, constraints, ON DELETE, typy danych. Porównanie z migracjami Prisma
2. Normalizacja — 1NF (atomowe wartości), 2NF (zależność od całego klucza złożonego), 3NF (brak transitive dependencies). Denormalizacja: snapshot pattern jako świadomy trade-off
3. Locking — optimistic (version/updatedAt check) vs pessimistic (SELECT FOR UPDATE). Kiedy który
4. Isolation levels — Read Uncommitted, Read Committed (domyślny PG), Repeatable Read, Serializable. Trade-off izolacja vs performance

## Planowanie architektoniczne

Nie dotyczy — sesja teorii + ćwiczenie SQL, nie feature development.

## Samodzielność (1-5)

3.5 — CREATE TABLE napisał sam z dobrymi decyzjami (UUID PK, DECIMAL na weight, ON DELETE CASCADE/SET NULL, NOT NULL na FK, order na setach). Potrzebował naprowadzenia na: TEXT vs VARCHAR, DECIMAL precision syntax, ON UPDATE CASCADE, updatedAt bez DEFAULT (ORM runtime), cudzysłowy w PostgreSQL ('' vs ""). Normalizacja, locking, isolation levels — nowe tematy, przyswojone z przykładami. Sam zauważył composite key w WorkoutTemplateExercise przy 2NF. Sam sprawdził docs zamiast czekać.

## Explain phase

(3.5/5) — DECIMAL uzasadniony trafnie (precyzja float). Snapshot trade-off — wiedział co traci (duplikacja, nieaktualność). Repeatable Read — poprawny scenariusz (dwa SELECTy w jednej transakcji). Odpowiedzi treściwe ale chaotyczne — brakuje struktury "wybrałem X bo Y, a nie Z bo [trade-off]".

## Mock interview

Pytanie: "Tabela orders z milionem rekordów, lista ładuje się 5 sek — jak diagnozujesz i naprawiasz?" Score: 3.5/5. Trafnie: EXPLAIN ANALYZE → Seq Scan → indeks, paginacja, zmniejszenie payloadu, sprawdzenie logów. Pominął: cache (Redis/HTTP), N+1 problem, connection pooling. Dobry flow diagnostyczny ale brakuje szerszego spojrzenia (nie tylko query, ale cache i transfer).

## Co poszło dobrze

- Obronił batch transaction z konkretnymi argumentami — uzasadnienie decyzji jak na rozmowie
- CREATE TABLE samodzielnie z dobrymi decyzjami architektonicznymi
- Sam sprawdził docs Prisma (context7) bez czekania na coacha
- Sam zauważył composite key w WorkoutTemplateExercise przy omawianiu 2NF
- Sam zauważył rozbieżność notes nullable w Prisma schema vs migracji
- Normalizacja + locking + isolation levels — 3 nowe tematy w jednej sesji, przyswojone
- Trafny feedback coachingowy (recall jako dialog, 🔴 zero przygotowanie, krok po kroku) → skill zaktualizowany
- Snapshot + denormalizacja — umiał uzasadnić trade-off biznesowo

## Co poszło źle

- Początkowy opór przy CREATE TABLE ("kto tak robi, wszyscy używają ORM") — po reframingu zabrał się do roboty
- Powtarzający się problem z syntax: przecinki po ostatniej kolumnie (3 razy), '' vs "" cudzysłowy
- Explain i mock — odpowiedzi treściwe ale chaotyczne, bez struktury narracji
- Mock interview — brakuje szerszego spojrzenia (cache, N+1) — pattern z poprzednich sesji: odpowiedzi poprawne ale płytkie
- Pomylił nazwy optimistic/pessimistic przy pierwszym podsumowaniu (opisał mechanikę poprawnie, nazwy odwrócił)

## Słabości — update

- **Explain — struktura narracji** — NADAL. 3.5/5 stabilne ale nie rośnie. Wie co i dlaczego, ale podaje chaotycznie. Potrzebuje ćwiczyć formułę: "wybrałem X bo Y, a nie Z bo [trade-off]"
- **Mock interview — szerokość opcji** — NADAL. Trafne ale płytkie odpowiedzi. Brakuje cache, N+1, connection pooling w repertuarze. Pattern: odpowiada na pytanie ale nie myśli "co jeszcze?"
- **SQL syntax details** — nowa obserwacja. Przecinki po ostatniej kolumnie, '' vs "" — drobne ale powtarzające się. Rozwiąże się z praktyką
- **Nazewnictwo konceptów** — pomylił optimistic z pessimistic (mechanikę znał, nazwy odwrócił). Do utrwalenia przez Anki

## Faza coachingu

Faza 1 (milestones 1-3). Samodzielność 3.5 — stabilna. Sesja głównie teoria + ćwiczenie SQL (nie feature dev). 3 nowe tematy z 🔴 zero przyswojone. Zostajemy w fazie 1. Milestone 2.

## Następna sesja

### Rekomendacja głównego taska
Kontynuacja M2 SQL: CREATE TABLE z pamięci — tym razem odpal w psql i napraw błędy. Potem: napisz raw SQL queries na danych IRONLOG (INSERT kilka logów, SELECT z JOINem, UPDATE, DELETE). Cel: checkpoint "Potrafię napisać CREATE TABLE z FK i constraints z pamięci". Alternatywa: jeśli Jakub chce kodować — poprawki z review (order w WorkoutLogExercise/Set w Prisma schema, notes nullable fix) + nowy endpoint.

### Obserwacje z sesji (input dla session-start)
- **Słabości do zaadresowania:** Explain chaotyczny (3.5/5 — stabilne, nie rośnie), mock interview płytkie odpowiedzi (brakuje cache/N+1 w repertuarze), pomylił nazwy optimistic/pessimistic
- **Poprawki z review:** order brakuje w WorkoutLogExercise i WorkoutLogExerciseSet (Prisma schema — nie bloker ale warto dodać), notes powinno być nullable (fix migracja już istnieje)
- **Co poszło dobrze:** Obronił batch transaction, CREATE TABLE samodzielnie, 3 nowe tematy w jednej sesji, trafny feedback coachingowy

### Retencja (tematy do weryfikacji)
- Normalizacja 1NF/2NF/3NF (nowe — pierwsza ekspozycja dziś)
- Optimistic vs pessimistic locking (nowe — pierwsza ekspozycja dziś, pomylił nazwy)
- Isolation levels (nowe — pierwsza ekspozycja dziś)

### Materiały do nauki przed sesją
- **Docs:** PostgreSQL docs → CREATE TABLE (constraints sekcja). Prisma docs → raw SQL queries ($queryRaw, $executeRaw)
- **Pytanie do przemyślenia:** Masz endpoint który generuje raport miesięczny (łączy dane z 3 tabel). Jaki isolation level wybierasz i dlaczego? A co z cachem — kiedy liczyć na żywo a kiedy cache'ować wynik?
