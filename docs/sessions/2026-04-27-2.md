# Sesja 2026-04-27-2

## Plan sesji
- **Czas:** 1.5h
- **Milestone:** M4 — Auth + Security 🔴 BLOKUJE
- **Main task:** Min. 2 unit testy dla AuthService (validateUser, register) — ostatni core M4 checkpoint
- **Rozgrzewka:** brak
- **Docs:** NestJS Testing → Unit testing (`Test.createTestingModule`, mockowanie zależności)

## Task briefing topics

brak

## Code review — internal memo (session-end input)

**Mapping do nauki:**
- CONCEPT GAP: `jest.clearAllMocks()` / izolacja mocków między testami — nie ma wpisu w banku (M7 testing topics gated, ale pattern warto zapamiętać)
- L3 ANCHOR: checkpoint "Min. 2 unit testy dla AuthService (validateUser, hashPassword)" — `src/auth/auth.service.spec.ts` — **niekompletny**: brakuje testu dla `register`. Checkpoint nie zalicza się dopóki register nie przetestowany.

**Pełna lista findings:**
- CRITICAL: brak `jest.clearAllMocks()` — mock state (call history, mockResolvedValue) nie resetuje się między testami; doda się test który sprawdza `.toHaveBeenCalledTimes` → błędny wynik
- CRITICAL: brak testu dla `register` — checkpoint mówi "validateUser, hashPassword/register", połowa zakresu brakuje
- ARCHITECTURAL: brak testu `validateUser` gdy user nie istnieje (findByEmailWithPassword zwraca null) — trzecia gałąź logiki nieprzetestowana
- ARCHITECTURAL: `beforeAll` zadeklarowany po `beforeEach` — niestandardowa kolejność, pułapka czytelności (nie bug dziś bo Jest i tak uruchamia beforeAll przed beforeEach)
- MINOR: 3 factory functions dla 2 testów — over-abstracted, plain inline objects byłyby czytelniejsze
- MINOR: `ConfigService/JwtService/AuthRepository` jako `{}` — time bomb przy przyszłych testach innych metod
- MINOR: `tsconfig.json` types powinny zawierać `["jest", "node"]` nie tylko `["jest"]`
- MINOR: ts-jest 29.x z jest 30.x — version mismatch, może generować warningi

## Odhaczone L3

- [x] Min. 2 unit testy dla AuthService (validateUser, hashPassword) — anchor `src/auth/auth.service.spec.ts`

## M4 zamknięty — wszystkie core checkpointy [x]

## Notatki na bieżąco

---

## Co robił

Napisał 4 unit testy dla AuthService: `validateUser` (happy path, wrong password, user not found) + `register` (hash verification). Używał `Test.createTestingModule` z `UsersService` jako jedynym mockiem, reszta jako empty `{}`. Naprawił shared mock state przez `afterEach(jest.clearAllMocks)`, wydzielił bcrypt hash do `beforeAll` z cost=1.

## Planowanie architektoniczne

Brak formalnego planowania — task był checkpointem z jasno zdefiniowanym zakresem. Jakub samodzielnie zaproponował mock UsersService + real bcrypt z cost=1 dla szybkości testów.

## Code review

Jakub sam nie zauważył nic ("nic mi nie przeszkadza"). Po sokratejskich pytaniach: clearAllMocks po podpowiedzi, brak register testu po pytaniu wprost. Wyniki scanu: 2 CRITICAL (clearAllMocks, brak register), 2 ARCHITECTURAL (brak user-not-found brancha, beforeAll po beforeEach). Wszystkie naprawione.

## Samodzielność (1-5)

**3/5** — Jakub napisał testy ale z dużą ilością trivialnych błędów setupu (wrong import `node:test`, wrong method name `findEmailWithPassword`, invalid hash `'hashedpassword'`). Logika testów była poprawna po wskazaniu kierunku. Naprawił samodzielnie po ukierunkowaniu.

## Explain phase

Wytłumaczył mocking rationale poprawnie ("nie musimy wykonywać realnej logiki"). Powiedział "zapisanie w bazie danych" bez korekty — poprawił się dopiero po pytaniu. Brak spontanicznego rozróżnienia unit/integration.

**Score: 3.5/5**

## Articulation check

- **bcrypt vs SHA256 (M4):** 3.5/5 (poprzednio 1.5) — interval 1d → 26d | anchor: src/auth/auth.service.ts:11
- **INNER vs LEFT JOIN (M2):** 3.5/5 (poprzednio 3.5) — interval 5d → 34d | anchor: none

Lapse notatki: brak
Nowe słabości (persistent): NULL w prawej tabeli LEFT JOIN — pojawia się po prompcie drugi raz z rzędu

## Nowe L2 topics (dodane mid-session)

`Test.createTestingModule()` (M7) — Jakub zaimplementował całą strukturę today, ale temat jest M7-gated. Bank ma już score-0 placeholder. L3 anchor zostanie zaktualizowany w banku gdy M7 aktywny: `src/auth/auth.service.spec.ts`.

## Co poszło dobrze

- Sam zaproponował bcrypt cost=1 dla szybkości testów — dobra intuicja bez podpowiedzi
- Poprawnie rozumie mocking rationale
- Naprawił clearAllMocks i register test po code review bez ghostwritingu
- bcrypt 1.5 → 3.5 po jednej sesji aktywnego użycia

## Co poszło źle

- Wiele trivialnych błędów setupu przed run — brak nawyku self-review kodu przed uruchomieniem
- "Zapisanie w bazie danych" w explain phase — unit vs integration distinction wymaga promptu
- Shared mock state nie zauważony samodzielnie podczas code review

## Słabości — update

- **Unit test setup errors** (NOWA): wrong imports, wrong method names, invalid test data przed run. Brak nawyku self-review.
- **Unit vs integration** (update): Jakub wie że mocki ≠ baza, ale spontaniczny opis idzie w stronę "bazy danych". Wymaga kolejnego cyklu.
- **NULL w LEFT JOIN** (persistent): pojawia się po prompcie 2. sesję z rzędu.

## Faza coachingu

Faza 2 (M4-M6). M4 zamknięty. Aktywny: M5. Gotowość na fazę 3: wymaga 4+ sesji z oceną samodzielności 4-5 — aktualnie 3/5.

## Następna sesja

### Rekomendacja głównego taska
M5 core checkpoint: **Stream processing endpoint** — `POST /workout-logs/import-csv` używający `fs.createReadStream` + `csv-parser`, przetwarzający duży plik bez OOM. Jedyny core `[ ]` w M5. Event loop (3.0) i Streams (3.5) mają podstawy w banku — task kotwicy teorię w kodzie.

### Task briefing — relevant score-0 topics
Z M5 (aktywny milestone), score-0 kandydaci:
- `Node 1000 concurrent requests — co się dzieje (M5)` — score 0, context dla streams
- `cluster vs worker_threads (M5)` — score 0, context skalowania
- `Operational errors vs programmer errors (M5)` — score 0, error handling w stream pipeline

### Obserwacje z sesji (input dla session-start)
- **Słabości do zaadresowania:** self-review przed run, unit vs integration terminology
- **Poprawki z review (rozgrzewka):** ts-jest 29.x + jest 30.x version mismatch (minor), `tsconfig.json` `["jest", "node"]` (minor) — 5 min
- **Co poszło dobrze:** bcrypt intuicja, mocking rationale, M4 zamknięty samodzielnie

### Materiały do nauki przed sesją
- **Docs:** Node.js Streams — `stream.Readable`, `stream.pipeline`, backpressure section
- **Pytanie do przemyślenia:** "Masz endpoint który przetwarza 100MB CSV. Klient wysyła POST i czeka. Co się dzieje z pamięcią procesu Node? Co zrobisz żeby nie zabić serwera?"
