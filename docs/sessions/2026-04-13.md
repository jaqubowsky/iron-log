# Sesja 2026-04-13

## Plan sesji
- **Czas:** 30 min (wydłużona o 2 tematy — w praktyce ~45-50 min)
- **Milestone:** M4 (backlog retencji ≥8 → TWARDY PRÓG → mock-interview session obowiązkowa)
- **Rozgrzewka:** brak
- **Main task:** Mock-interview session — planowane 2 tematy, wydłużone do 4 na prośbę Jakuba
- **Docs:** brak (sesja artykulacyjna)

## Recall challenge

Pytanie: Cache-Control headers — max-age/no-cache/no-store/stale-while-revalidate + walidacja + header dla GET /exercises (temat z sesji teorii 2026-03-31, nigdy nie testowany formalnie recallem, 13 dni spacing).

Szkielet samodzielnie: max-age OK, no-cache rozpoznał jako "waliduj zanim serwuj", no-store OK, stale-while-revalidate wytłumaczony poprawnie w połączeniu z max-age. Brakowało: nazwy request headerów (If-None-Match, If-Modified-Since) — nie padły samodzielnie, public/private distinction pominięty. Re-recall po feedbacku: domknął oba braki (If-None-Match + 304, `public, max-age=3600, stale-while-revalidate=86400`).

**Score: 3.5/5 — zostaje w retencji** (pierwsza produkcja = właściwa miara, re-recall tylko potwierdza wbudowanie feedbacku).

## Co robił

Sesja artykulacyjna — 4 tematy mock interview z backlogu M2/M3/M4 (zaplanowane 2, wydłużone na prośbę Jakuba). Tematy: Cache-Control (recall), JWT access+refresh flow, API versioning, Idempotentność HTTP, N+1 problem. Bez kodowania.

## Odhaczone checkpointy (próg 4/5)

- [x] Potrafię wytłumaczyć access token + refresh token flow i co gdy token wycieknie (4/5, M4) — progres od 3.5/5 z 2026-04-04

## Pozostałe w retencji (score aktualizowany)

- Cache-Control headers — 3.5/5 → `⏳ retencja 3.5/5 — 2026-04-13`
- API versioning — 3/5 → `⏳ retencja 3/5 — 2026-04-13` (spadek z 3.5/5 — ale to pierwszy formalny recall)
- Idempotentność HTTP — 3.5/5 → `⏳ retencja 3.5/5 — 2026-04-13`
- N+1 problem — 3/5 → `⏳ retencja 3/5 — 2026-04-13`

## Planowanie architektoniczne

N/A — sesja artykulacyjna.

## Code review

N/A — sesja artykulacyjna.

## Samodzielność (1-5)

N/A — mock-interview session. Ogólna ocena: 3.5/5. Odpowiedzi samodzielne z 1-2 promptami dopytującymi na temat. Re-recall po feedbacku domykał braki bez dodatkowej pomocy. Zero prób ghostwritingu.

## Explain phase (skrócony — sesja artykulacyjna)

Zapytany o najtrudniejszy temat dnia + jedną rzecz do zapamiętania. Najtrudniejszy: N+1 (brak wiedzy o DataLoader i detection tools — legit luka). Do zapamiętania: API versioning — header versioning używany w Stripe, działa z `Vary: Accept` ale cache hit rate spada (per-klient zamiast per-resource). **Score: 4/5** — trafna auto-ocena, wyciągnął insight który faktycznie zostanie.

## Mock interview

Pominięty — sesja była mock-interview session (4 tematy w jednym bloku).

## Co poszło dobrze

- **JWT flow 4/5 — odhaczone.** Miesiąc temu wymagałoby 3 naprowadzeń, dzisiaj Jakub opowiedział flow, lifetime, storage trade-offs, rotation + reuse detection samodzielnie. To jest największa wygrana sesji — pytanie pada na ~80% rozmów backendowych mid+.
- **Prisma `include` = SELECT + IN, nie JOIN** — zaawansowana wiedza o ORM wymieniona samodzielnie bez promptu. Większość devów tego nie wie. Daje realną przewagę na technical interview.
- **PATCH niuans (update vs increment idempotency)** — wymyślony samodzielnie w temacie Idempotentności. To jest poziom mid+.
- **"PUT wymaga znanego ID przy create" — kanoniczny argument dla idempotency key** — wymyślony w re-recall bez podpowiedzi. Pokazuje że Jakub rozumie *dlaczego* REST semantyka zmusza do tego patternu, nie tylko *że* tak się robi.
- **Headers Cache-Control dla /exercises** — po feedbacku poprawnie: `public, max-age=3600, stale-while-revalidate=86400`. Rozumie że shared resource potrzebuje `public`.
- **Auto-ocena w explain phase** — trafna i honest (N+1 jako najtrudniejszy bo luka w wiedzy, a nie słabość).

## Co poszło źle

- **Idempotency key 409 jako default** — Jakub nie znał pattern "200 + cached response" (Stripe standard). Zwrócił 409 przy duplikacie klucza zamiast cached replay. To jest realna nieścisłość do domknięcia homeworkiem.
- **API versioning — "header versioning = brak cache"** — niepoprawne. Vary: Accept mechanism pominięty w pierwszym recallu, po feedbacku zrozumiany powierzchownie ("nie każdy CDN wspiera") bez drugiego trade-offu (cache hit rate).
- **Non-breaking vs breaking changes** — pominął całkowicie mimo wyraźnego dopytania. To jest core API evolution wiedza która na rozmowie pada wprost.
- **DataLoader + N+1 detection tools** — białe pole, nigdy nie omawiane. To nie "słabość" tylko luka do zapełnienia homeworkiem.
- **Dwie sesje artykulacyjne z rzędu** — rytm sesji mocno przesunięty w stronę teorii. Backlog retencji spadł tylko o 1 netto (JWT). Potrzebny powrót do kodowania M4 dla balansu.

## Słabości — update

- **Precyzja bez scaffoldingu** — NADAL, ale z mniejszą siłą niż 2026-04-10. Re-recalle tym razem domykały braki w całości (Cache-Control headers, PUT-ID argument dla idempotency). Feedback zaczyna być wbudowywany w czasie re-recallu, nie tylko zapamiętywany na potem.
- **Standardy branżowe (Stripe patterns)** — NOWA obserwacja. Jakub zna mechanizmy ale nie zna jak je stosują duże API publiczne. Idempotency key (409 vs 200), API versioning (Stripe header), non-breaking change philosophy — te trzy rzeczy razem sugerują potrzebę **czytania real-world API docs** jako regularnego homework, nie tylko NestJS docs.
- **DataLoader + detection tools** — NOWA luka w M3. Nigdy nie omawiane formalnie. Do zrobienia w M7 (testy/production) lub wcześniej jeśli pojawi się w kontekście performance debugowania.

## Faza coachingu

Faza 1 (M1-M3 zakończone, M4 aktywny). Samodzielność na teorii stabilna 3.5-4. Przejście do Fazy 2 wymaga sesji **kodowych** z oceną 4-5 — ostatnia sesja kodowa to 2026-04-04 (9 dni temu). Potrzeba powrotu do kodowania żeby w ogóle wygenerować dane do oceny fazy.

## Następna sesja

### Rekomendacja głównego taska

**Kodowanie M4 — `POST /auth/register`** (endpoint: email, password, bcrypt hash, zapis do DB, 409 przy duplikacie email).

**Uzasadnienie odejścia od twardego progu backlogu:**

Backlog retencji to **12 checkpointów**, próg mówi "≥8 → mock-interview session obowiązkowa". ALE:
1. **Dwie sesje artykulacyjne z rzędu (2026-04-10, 2026-04-13).** Trzecia z rzędu to anti-pattern — sesje teorii mają sens jako rotacja z kodowaniem, nie jako blok.
2. **JWT flow odhaczone 4/5.** Mental model M4 Auth jest gotowy — Jakub rozumie flow, storage, rotation. Kodowanie nie odbywa się na pustej głowie.
3. **Bcrypt homework zaległy od 2026-04-10.** Drugi raz nie może się zdarzyć. Jeśli w session-start zobaczysz że Jakub NIE przeczytał bcrypt docs — **przesuń główny task** na inny M3 checkpoint z retencji i zostaw kodowanie M4 na kolejną sesję.
4. **Backlog nie eksploduje przez jedną sesję kodowania.** Retencja spadnie z 12 do 12 (nic nie dojdzie), można zrobić mock-interview session za 2-3 sesje kodowania.

**Override warunkowy:** Session-start — jeśli Jakub nie zrobił homework (bcrypt docs), NIE rób `POST /auth/register`. Zamień na sesję teorii z pozostałych retencji LUB na dedykowaną 30-min sesję "razem czytamy NestJS Security docs" (ekspozycja przed implementacją zamiast frustracji).

### Obserwacje z sesji (input dla session-start)

- **Słabości do zaadresowania:**
  - Standardy branżowe — Jakub zna mechanizmy ale nie zna jak Stripe/GitHub je stosuje. Warto włączać real-world API examples w code review i tłumaczeniach.
  - Non-breaking vs breaking changes (API evolution) — core wiedza, pominięta w recall. Następny mock interview pytaj o to.
- **Poprawki z review:** brak (sesja teorii)
- **Co poszło dobrze:** JWT flow — portfolio-ready. Prisma `include` internals — advanced. PATCH idempotency niuans — mid+ level.

### Retencja (tematy do weryfikacji na kolejnej sesji)

1. **Idempotentność HTTP + idempotency key** — 3.5/5 po sesji, Jakub ma domknąć 409 vs 200 przez Stripe docs homework. Recall na następnej sesji: "co Stripe zwraca przy duplikacie idempotency key?"
2. **N+1 problem + detection** — 3/5, luki w detection i DataLoader — Jakub ma domknąć przez IRONLOG Prisma logging + DataLoader README. Recall: "pokaż co widzisz w logach Prismy gdy robisz GET /workout-logs".

### Materiały do nauki przed sesją

- **Docs (CRITICAL — zaległe homework):** NestJS Security → Authentication (Passport local strategy) + bcrypt Hashing. Bez tego nie zaczynasz `POST /auth/register`.
- **Docs (homework z dzisiaj):** Stripe idempotent requests (10 min), DataLoader README (15 min), Prisma query logging w IRONLOG (5 min praktyki).
- **Pytanie do przemyślenia:** Register endpoint — gdzie sprawdzasz duplikat emaila: przed hashem czy po zapisie z unique constraint? Jakie są konsekwencje każdej kolejności (timing attack, wasted CPU na hash, race condition)?
