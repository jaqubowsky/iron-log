# Sesja 2 — 2026-03-27

## Plan sesji
- **Czas:** 1h
- **Milestone:** M2 (⏳ retencja) → M3 (🔴 BLOKUJE — HTTP/REST teoria)
- **Rozgrzewka:** poprawki z review — order w WorkoutLogExercise/Set (Prisma schema), notes nullable fix
- **Main task:** CREATE TABLE w psql + raw SQL queries na danych IRONLOG (INSERT, SELECT z JOIN, UPDATE, DELETE). Cel: odhaczenie checkpointu "Potrafię napisać CREATE TABLE z FK i constraints z pamięci"
- **Temat poboczny:** INNER vs LEFT JOIN ćwiczenie praktyczne
- **Docs:** PostgreSQL docs → CREATE TABLE constraints

## Recall challenge

Pytanie: "Dodajesz filtrowanie po kategorii do offset i cursor pagination — jakie problemy?" (mock interview 2026-03-25, 3/5). Score: 3.5/5 — Od razu trafił w kluczowy punkt: offset wymaga synchronizacji WHERE w count i findMany, cursor nie potrzebuje totalCount więc prostsze. Ghost pages — trafny scenariusz. Brakowało: composite cursor przy nie-unikalnym sort field (np. createdAt), świadomości że cursor radzi sobie z usuwaniem rekordów lepiej niż offset (WHERE id > cursor działa nawet gdy cursor usunięty).

## Co robił

1. Rozgrzewka — sprawdzenie schema Prisma, order i notes już były poprawione, prisma validate OK
2. CREATE TABLE z pamięci — WorkoutLog, WorkoutLogExercise, WorkoutLogExerciseSet. FK, constraints, UUID, DECIMAL. Pisał w pliku SQL z extension VS Code
3. INSERT — dodał exercise'y i workout log z ćwiczeniem i setami w psql
4. SELECT z JOIN — pobrał log z ćwiczeniami i setami (INNER JOIN)
5. UPDATE — zmienił wagę w secie (125.5 → 137.5)
6. DELETE — usunął log, zweryfikował CASCADE (exercises i sets zniknęły)
7. INNER vs LEFT JOIN — ćwiczenie z seedowanymi danymi. Zrozumiał różnicę, dał konkretne use case'y (stats = INNER, lista wszystkich = LEFT)

**Odhaczone checkpointy:**
- [x] Potrafię napisać CREATE TABLE z FK i constraints z pamięci — checkpoint praktyczny ("potrafię napisać"), core poprawny (FK, constraints, typy). Jakub słusznie zauważył że "napisać" ≠ "wytłumaczyć" i nie wymaga recall na kolejnej sesji. Skill session-end zaktualizowany o jasne rozróżnienie typów checkpointów

## Planowanie architektoniczne

Nie dotyczy — sesja ćwiczeniowa SQL, nie feature development.

## Samodzielność (1-5)

3 — CREATE TABLE napisał poprawnie z pamięci (UUID, FK, CASCADE/SET NULL, DECIMAL), ale potrzebował naprowadzenia na: brakujący `order` w dwóch tabelach, `reps` zamiast `sets` (pomyłka nazwy kolumny), DECIMAL precision. INSERT/SELECT/UPDATE/DELETE zrobił sam. Próbował wyciągnąć seed i timestampy od coacha — odmówione. JOIN exercise czyste — od razu wiedział kiedy LEFT vs INNER.

## Explain phase

(3.5/5) — CREATE TABLE poprawki zidentyfikował po naprowadzeniu (order, reps). LEFT vs INNER JOIN z konkretnymi use case'ami (stats vs lista wszystkich). CASCADE kontrprzykład trafny (usunięcie ćwiczenia z szablonu nie powinno kasować ćwiczenia). Nadal odpowiada listą faktów zamiast narracją z flow myślenia.

## Mock interview

Pytanie: "GET /workout-logs/:id trwa 200ms — jak diagnozujesz?" Score: 3.5/5. Trafnie: EXPLAIN ANALYZE, sprawdzenie indeksów, N+1, payload. Sam wyeliminował fałszywy trop (PK = automatyczny indeks). Brakowało: connection pooling (nowa wiedza — nie znał konceptu), middleware/cold start overhead. Progres: lepsze rozumowanie diagnostyczne niż poprzedni mock (eliminuje fałszywe tropy zamiast listy).

## Co poszło dobrze

- CREATE TABLE z pamięci — FK, CASCADE/SET NULL, UUID, DECIMAL — solidne fundamenty
- Sam wyeliminował fałszywy trop w mock interview (PK ma indeks, więc to nie przyczyna)
- LEFT vs INNER JOIN — od razu zrozumiał, dał konkretne IRONLOG use case'y
- CASCADE weryfikacja w psql — zrozumiał łańcuch usuwania praktycznie

## Co poszło źle

- Opór na początku ("znowu raw sql???") — zrozumiały ale marnuje czas
- Próby wyciągnięcia boilerplate od coacha (seed, timestampy) — INSERT to część ćwiczenia
- Pomylił `sets` z `reps` w nazwie kolumny — brak uwagi
- Pominął `order` w dwóch tabelach — nie porównał z Prisma schema przed wysłaniem
- Explain nadal lista faktów, nie narracja z flow myślenia

## Słabości — update

- **Explain — struktura narracji** — NADAL. 3.5/5 stabilne ale nie rośnie. Lista faktów zamiast "najpierw X bo Y, jeśli nie to Z". Potrzebuje ćwiczyć flow diagnostyczny na głos
- **Mock interview — szerokość opcji** — poprawa. Sam wyeliminował fałszywy trop (PK indeks) ale nadal brakuje warstw poza DB (connection pooling, middleware). Pattern: myśli głównie o query, nie o infrastrukturze
- **Dbałość o detale w SQL** — powtarzające się: pomyłka nazwy kolumny (sets vs reps), brak order. Rozwiąże się z praktyką ale widać że pisze na szybko bez weryfikacji

## Faza coachingu

Faza 1 (milestones 1-3). Samodzielność 3 — niżej niż ostatnio (3.5), bo sesja ćwiczeniowa wymagała więcej naprowadzenia na detale. Milestone 2 — praktyczne SQL checkpoints zamykane. Zostajemy w fazie 1.

## Następna sesja

### Rekomendacja głównego taska
M3 HTTP/REST teoria — checkpointy 🔴 zero blokują milestone 3. Rekomendacja: sesja na idempotentność HTTP metod + REST vs GraphQL + "co się dzieje gdy wpiszesz URL w przeglądarkę". Uzasadnienie: M2 SQL praktyka zamknięta (CREATE TABLE, JOINy, CRUD — ćwiczone), zostaje retencja przez Anki/recall. M3 kodowe checkpointy odhaczone (repo, paginacja, cross-module), ale 7 checkpointów 🔴 zero z teorii HTTP/REST blokują dalszy progres. Te tematy to fundamenty rekrutacyjne — pytane na każdej rozmowie. Alternatywa: N+1 problem (M3) z praktycznym przykładem w Prisma — łączy SQL z ORM.

### Obserwacje z sesji (input dla session-start)
- **Słabości do zaadresowania:** Explain nadal lista faktów (3.5/5 stabilne, nie rośnie). Mock — myśli o query ale nie o infrastrukturze (brak connection pooling w repertuarze). SQL detale (nazwy kolumn, brak order) — brak weryfikacji przed wysłaniem
- **Poprawki z review:** brak nowych — schema czysta
- **Co poszło dobrze:** CREATE TABLE z pamięci poprawne, LEFT vs INNER JOIN z konkretnymi use case'ami, sam wyeliminował fałszywy trop w mock interview

### Retencja (tematy do weryfikacji)
- Normalizacja 1NF/2NF/3NF (pierwsza ekspozycja 2026-03-27 sesja 1, ⏳ 3.5/5)
- Optimistic vs pessimistic locking (pierwsza ekspozycja 2026-03-27 sesja 1, ⏳ 3/5, pomylił nazwy)
- NestJS modules exports/imports (mock 2026-03-26, 2.5-3/5 — wiszący temat)

### Materiały do nauki przed sesją
- **Docs:** MDN → HTTP methods (GET, POST, PUT, PATCH, DELETE — idempotentność), MDN → HTTP response status codes (200, 201, 204, 301, 400, 401, 403, 404, 409, 500). REST API design best practices (dowolny artykuł)
- **Pytanie do przemyślenia:** Masz endpoint POST /workout-logs który tworzy log. User klika "Zapisz" 2 razy przez lagujący internet. Co się stanie? Jak temu zapobiec? (hint: idempotency key)
