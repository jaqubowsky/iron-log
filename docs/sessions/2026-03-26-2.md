# Sesja 2 — 2026-03-26

## Recall challenge

Pytanie: "Wytłumacz ACID — każda litera z przykładem z IRONLOG. Szczególnie: czym jest Consistency?" (temat z sesji 2026-03-25, explain 3.5/5 — Consistency słabe). Score: 3.5/5 — Duży progres na Consistency ("z poprawnego stanu do poprawnego stanu" — prawidłowa definicja). Constraints i rollback jako mechanizm — trafne. Isolation trochę uproszczone ("nie ma dostępu do wyniku drugiej" — w praktyce zależy od isolation level). Brakowało przykładów z IRONLOG. Przyznał się do podglądania nazw (A/C/I/D) — na tym etapie OK, ważniejsze jest zrozumienie niż nazwy.

## Mini-recall

- "Co to jest Dependency Injection i po co?" — Poprawnie: wstrzykiwanie zależności, modularyzacja, testowalność, brak couplingu. Pominął kto zarządza instancjami (DI container).

## Co robił

1. Schema Prisma: WorkoutLog, WorkoutLogExercise (snapshot + FK do Exercise), WorkoutLogExerciseSet — z poprawnym ON DELETE (CASCADE na setach/exercises, SET NULL na template/exercise FK)
2. DTO: CreateWorkoutLogDTO z ValidateIf (conditional validation), UpdateWorkoutLogDTO, response DTO
3. Interfejsy: CreateWorkoutLogPayload, UpdateWorkoutLogPayload z shared WorkoutLogExercise
4. Repository: PrismaWorkoutLogRepository z nested create, transakcyjny update (delete+create)
5. Service: orkiestracja — resolveExerciseIds (1 roundtrip zamiast 2), mapowanie DTO→payload przez Mapper, dwa flow (z/bez template)
6. Controller: CRUD endpoints z response mapping
7. Module: cross-module imports (ExercisesModule, WorkoutsModule)
8. Poprawki z code review: cross-module wiring (import zamiast ręcznej rejestracji providerów), literówka @Delete, originalExerciseId w interfejsie/response DTO
9. Feedback: coach powinien naprowadzać pytaniami, nie dawać wprost odpowiedzi — zaktualizowane CLAUDE.md i code-review skill

## Planowanie architektoniczne

Dobre. Jakub sam doszedł do: osobny moduł (bo log może istnieć bez template), SET NULL na obu FK, snapshot ćwiczeń, osobna tabela na sety. Potrzebował naprowadzenia na: FK do Exercise (dla analityki — "co jeśli chcesz zobaczyć progresję na squacie?"), relacja one-to-many vs many-to-many (początkowo chciał pivot table gdzie FK wystarczy — sam się poprawił po pytaniu naprowadzającym). Nie wymyślił od razu kolejności implementacji — potrzebował promptu. Plan ~15 min — adekwatny do złożoności.

## Code review

2 rundy. Kluczowe:
- **Runda 1 (mid-session):** Schema + DTO review. Jakub sam zauważył brak spójności w nazewnictwie. Coach naprowadził pytaniami na: brak order w WorkoutLogExercise, zły response DTO name, name/description w CreateDTO vs schema mismatch. Agenci flagged: dead imports, @ArrayNotEmpty drop w UpdateDTO.
- **Runda 2 (finalna):** Pełny scan. Jakub sam zauważył: brak rozgraniczenia DTO/types, naming WorkoutsModule vs WorkoutTemplatesModule. Agenci flagged: @Delete('id') literówka, originalExerciseId mapping bug, batch vs interactive transaction, notes type mismatch, brak orderBy. Cross-module wiring naprawiony w trakcie review (ręczna rejestracja → imports).

## Samodzielność (1-5)

3.5 — Schema design samodzielny z dobrymi decyzjami (SET NULL, snapshoty, osobne tabele). Repository i service napisane sam. Potrzebował naprowadzenia na: one-to-many vs many-to-many (pivot table reflex), FK do Exercise (analityka), nested create w Prisma (nie znał `create` z tablicą), cross-module wiring (importy zamiast ręcznej rejestracji providerów), separation of concerns (service orkiestruje, nie repository). Dużo czasu na dyskusje o strukturze folderów/naming/null vs undefined kosztem kodowania.

## Explain phase

(3/5) — Opisał co zrobił na wysokim poziomie (moduł, schema, relacje) ale chaotycznie i bez uzasadnień. Na pytania follow-up odpowiedział trafnie: snapshot + FK razem (analityka + historyczny zapis), SET NULL (log żyje bez template), dwa flow (ad-hoc trening). Ale nie potrafił tego opowiedzieć sam bez pytań naprowadzających. Na rozmowie nikt nie pomoże pytaniami.

## Mock interview

Pytanie: "Moduł A potrzebuje danych z modułu B — jakie masz 3+ opcje architektoniczne z trade-offami?" Score: 3/5. Od razu DI/import. Event-based po hincie (pomylił z Event Sourcing — poprawiony). Coach błędnie naliczył FK constraint jako trzecią opcję (FK to walidacja integralności, nie dostęp do danych — nie odpowiada na pytanie). Jakub słusznie to zakwestionował. Nadal brakuje samodzielnego myślenia o alternatywach ale progres vs jedna opcja wcześniej.

## Co poszło dobrze

- Schema design samodzielny — SET NULL, snapshoty, osobna tabela na sety, dobre decyzje
- Sam zauważył problem z many-to-many vs one-to-many po pytaniu naprowadzającym — i sam to zapamiętał
- Sam zauważył niespójność naming i brak rozgraniczenia DTO/types — dojrzałe oko architektoniczne
- Cross-module wiring naprawiony po zrozumieniu zasady (własny provider = providers, cudzy = imports)
- resolveExerciseIds — sam wpadł na optymalizację (1 roundtrip zamiast 2)
- Trafny feedback coachingowy — "naprowadzaj pytaniami, nie mów wprost" — dojrzałe podejście do nauki
- Mapper wydzielony z reużywalnym toExercisesPayload

## Co poszło źle

- Many-to-many vs one-to-many — odruchowe sięganie po pivot table bez analizy relacji
- Dużo czasu na dyskusje o folderach/naming/null vs undefined (~20 min) kosztem kodowania
- Mock interview 2.5/5 — ten sam problem co poprzednio: jedna opcja, brak alternatyw
- Explain chaotyczny — nie potrafi opowiedzieć architektury w 2 minuty bez pytań
- Batch vs interactive transaction — nie znał różnicy, krytyczny bug w update
- Controller literówka (@Delete('id') zamiast ':id') i zamienione pola w response mapping

## Słabości — update

- **Mock interview — liczba opcji** — NADAL główna słabość. 3 sesje z rzędu ten sam problem. Tylko jedna opcja od razu, reszta po hintach. Wymaga deliberate practice: przed każdą decyzją myśl "jakie 2-3 inne sposoby?"
- **Many-to-many vs one-to-many** — NOWA słabość. Odruchowo sięga po pivot table. Zasada do utrwalenia: "A→wiele B I B→wiele A?" — zapisane w memory
- **Explain — struktura narracji** — pogorszone vs poprzednie sesje (3/5 vs 3.5/5). Dużo wiedzy, chaotyczne podawanie. Potrzebuje ćwiczyć: "Zrobiłem X, bo Y, a nie Z, bo [trade-off]"
- **Prokrastynacja przez dyskusje o strukturze** — nowa forma: naming, foldery, null vs undefined. Wartościowe tematy ale zjadają czas kodowania
- **SQL fundamenty** — nie ćwiczone. ACID recall 3.5/5 — progres ale nadal nieodhaczone
- **Batch vs interactive transactions** — nowa wiedza, wymaga utrwalenia

## Faza coachingu

Faza 1 (milestones 1-3). Samodzielność 3.5 — stabilna. Nowy moduł end-to-end z wieloma warstwami (schema, DTO, interfaces, mapper, repo, service, controller, module). Schema design samodzielny. Cross-module zrozumiany po naprowadzeniu. Zostajemy w fazie 1. Milestone 3.

## Następna sesja

### Rekomendacja głównego taska
Rekomendacja: SQL fundamenty z zerową ekspozycją (M2 `🔴 zero`). Uzasadnienie: M2 ma 4 tematy z zerową ekspozycją które BLOKUJĄ dalszy progres — normalizacja (1NF/2NF/3NF), locking (optimistic vs pessimistic), isolation levels, CREATE TABLE od zera. Jakub kodowo jest silny (3 moduły CRUD), ale te fundamenty SQL nigdy nie były na sesji. Session-start powinien rozważyć dedykowaną sesję na 1-2 z tych tematów (np. normalizacja + isolation levels — naturalnie łączą się z ACID który Jakub już zna na 3.5/5). Alternatywa: HTTP/REST teoria z M3 (`🔴 zero`) — ale M2 fundamenty mają priorytet nad M3 teorią.

### Obserwacje z sesji (input dla session-start)
- **Słabości do zaadresowania:** Explain chaotyczny (3/5 — pogorszenie), mock interview nadal 1 opcja (3 sesje z rzędu), many-to-many vs one-to-many (nowa słabość), prokrastynacja przez dyskusje o naming/folderach
- **M2 `🔴 zero` blokery:** normalizacja, locking, isolation levels, CREATE TABLE — nigdy nie przerabiane, blokują milestone 2
- **Poprawki z review:** interactive transaction zamiast batch w update (bloker — potencjalny bug), orderBy w findMany/findUnique (kosmetyczna), spójność delete response message vs full object (kosmetyczna), findAll lightweight osobny return type (kosmetyczna)
- **Co poszło dobrze:** Schema design samodzielny, resolveExerciseIds optymalizacja, sam zauważył niespójności w naming, cross-module wiring zrozumiany

### Retencja (tematy do weryfikacji)
- NestJS modules exports/imports (2.5/5 na mock — priorytet)
- ACID (3.5/5 — poprawia się ale nadal nieodhaczone)
- cross-module alternatywy: event-based vs DI (3/5 na mock — pomylił z Event Sourcing)

### Materiały do nauki przed sesją
- **Docs:** Prisma docs → interactive transactions ($transaction with callback). NestJS docs → Modules (exports/imports — powtórz, na mock interview słabo wypadło)
- **Pytanie do przemyślenia:** Masz 3 opcje komunikacji między modułami (DI import, event-based, FK constraint). Dla każdej wymień: kiedy używasz, jakie trade-offy, jaki coupling. Ćwicz mówienie tego na głos w 1 minutę
