# Sesja 2026-03-20 (sesja 2)

## Co robił
Error handling — napisał custom Prisma Exception Filter (łapie P2000, P2002, P2025 i mapuje na HTTP statusy). Podpiął globalnie w main.ts. Dodał whitelist do ValidationPipe. Zamienił findUnique + ręczny NotFoundException na findUniqueOrThrow dla spójności. Poprawił DTO (MaxLength zamiast Max, dodał @IsOptional).

## Samodzielność (1-5)
3.5 — Przeczytał docs przed sesją (postęp vs poprzednie sesje). Przyszedł z sensowną opinią o try/catch vs filter. Cały kod pisał sam. Potrzebował naprowadzania na: różnicę Record<string, ...> vs typowany klucz, Max vs MaxLength, @IsOptional, findUniqueOrThrow. Sam znalazł findUniqueOrThrow w docs Prisma.

## Co poszło dobrze
- Przeczytał exception-filters docs PRZED sesją — pierwszy raz odrobił "pracę domową"
- Przyszedł z przemyślaną opinią (filter = ostatnia deska ratunku, try/catch = precyzja) — dobry mental model
- Sam napisał type guard z exhaustive check (PrismaError union + never) — zaawansowany TS pattern
- Sam zauważył findUniqueOrThrow i zaproponował zmianę na spójność
- Szybko debugował — logował exception.meta zamiast zgadywać

## Co poszło źle
- Nie znał różnicy Max vs MaxLength — nie sprawdził co dekorator robi przed użyciem
- Nie wiedział że findUnique zwraca null a nie rzuca — trzeba było naprowadzić
- Początkowa rejestracja filtra jako zwykły provider (custom token) zamiast useGlobalFilters — nie doczytał docs do końca
- Message w P2002 generyczny ("violating fields") zamiast użyć danych z exception.meta — trochę leniwy

## Słabości — update
- **Next.js magic assumptions** — nie pojawiło się dziś
- **Focus** — lepiej niż poprzednio, trzymał się zadania
- **Czytanie docs do końca** — nadal powierzchownie. Wie że Exception Filter istnieje, ale nie doczytał jak rejestrować globalnie. Poprawia się (przeczytał przed sesją) ale nadal skimuje zamiast czytać dokładnie
- **Sprawdzanie co API/dekorator robi** — używa bez sprawdzenia (Max na stringu). Trzeba wyrobić nawyk: "co ten dekorator przyjmuje i co robi?"

## Faza coachingu
Faza 1 (tydzień 1-3). Progres: odrobił pracę domową, przyszedł z opinią, sam pisze bardziej zaawansowany TS. Naprowadzanie nadal potrzebne ale coraz mniej. Jeszcze 2-3 sesje na tym poziomie i można próbować fazę 2.

## Następna sesja
- Uporządkować ExercisesModule — upewnić się że CRUD jest kompletny, przetestowany, spójny
- Przeglądnąć roadmapę: checkpointy tygodnia 1-2 — co zostało do odhaczenia?
- Zastanowić się nad error handling edge cases: co jak klient wyśle pusty body? Co jak Prisma rzuci NIEZNANY błąd (nie P2000/P2002/P2025)?
