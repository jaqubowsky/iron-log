# Sesja 2026-04-19

## Plan sesji
- **Czas:** 1.5h
- **Milestone:** M4 — Auth + Security 🔴 BLOKUJE
- **Main task:** `JwtRefreshStrategy` + `POST /auth/refresh` — największy remaining M4 task
- **Rozgrzewka:** dead import `JwtAuthGuard` w `auth.controller.ts`, `Cookie` decorator `as string` → `string | undefined` (~5 min)
- **Docs:** brak (JwtStrategy pattern już znany, cookie extraction = wariant tego co Jakub pisał w logout)

## Task briefing topics

brak

## Co robił

`RefreshTokenGuard` (custom guard zamiast passport-custom — deprecated package) + `POST /auth/refresh/token`. Architektoniczne decyzje: passport-custom → odrzucony (5 lat bez update), signed JWT refresh → odrzucony (migration cost), custom guard — wybrany (zero deps, pełna kontrola). Guard czyta cookie, hashuje, DB lookup z `include: { user: true }` (1 query), ustawia `request.user`. Service `refreshAuthTokens` revokuje stary token, tworzy nowe. Runtime verified: 200 happy path, 401 reuse detection, 401 no cookie.

## Odhaczone L3

- [x] `RefreshTokenGuard` implementowana — anchor `src/auth/guards/refresh-token-guard.ts:1`
- [x] `RefreshTokenGuard` chroni `POST /auth/refresh/token` — anchor `src/auth/auth.controller.ts:83`
- [x] `POST /auth/refresh/token` — rotacja, nowy access token — anchor `src/auth/auth.controller.ts:83`

## Code review — internal memo (session-end input)

**Mapping do nauki:**
- CONCEPT GAP: TOCTOU — Jakub nie wiedział o race condition między guard validate a service revoke. Zamknął przez `updateMany count > 0` check.
- L3 ANCHOR: `POST /auth/refresh/token` → `src/auth/auth.controller.ts:83`

**Pełna lista findings:**
- CRITICAL (fixed): TOCTOU race condition — validate w guard + revoke w service bez atomowości. Fix: `revokeRefreshToken` zwraca `boolean` (count > 0), serwis rzuca `UnauthorizedException` przy count=0
- ARCHITECTURAL (fixed): `throw new Error('User not found')` → 500. Zmienione na `UnauthorizedException`
- ARCHITECTURAL (open): `include: { user: true }` w `prisma-auth-repository.ts` fetchuje wszystkie pola usera włącznie z `passwordHash`. Powinno być `include: { user: { select: { id: true, email: true } } }`
- MINOR (open): `if (!data.refreshToken)` w `validateRefreshToken` service — dead code (guard już to sprawdza)
- MINOR (open): `*-input.ts`, `*-output.ts` naming w interfaces — Jakub sam zauważył, niespójność z resztą projektu. Do ustalenia przy następnym review
- MINOR (open): `@JWTUser` decorator na route gdzie user pochodzi z custom guard (nie z JWT) — semantycznie myląca nazwa, działa przez coincidence field name `id`
- MINOR (open): route `/refresh/token` vs konwencja `/refresh`

## Planowanie architektoniczne

Sporo czasu na decyzję architektoniczną: passport-custom → odrzucony (5 lat bez update), signed JWT refresh → odrzucony (migration cost), custom guard wybrany. Jakub samodzielnie zidentyfikował że opaque UUID ≠ JWT → guard zamiast Passport strategy. Dobra jakość reasoning.

## Samodzielność (1-5)

**4** — RefreshTokenGuard zaimplementowany samodzielnie po ustaleniu architektury. `@Public()` bug znaleziony przez pytanie. TOCTOU race condition — wymagał naprowadzenia przez pytanie, ale po 1 pytaniu od razu trafne rozwiązanie (count check w updateMany). Explain phase 3.5/5 — obie kluczowe decyzje padły po promptach.

## Explain phase

**3.5/5** — "customowy guard zamiast Passport bo opaque token" po pytaniu. "Two concurrent requests — zabezpieczeni przez updateMany count" po pytaniu. Plain language OK, nie szczegółowość.

## Articulation check

- **Prisma → SQL mapping** (M2): 4.5/5 (poprzednio 4.0) — 2 osobne queries (SELECT + SELECT IN), merge w pamięci, WHY nie JOIN. Interval 10d → 70d.
- **REST sparse fields** (M3): 3.0/5 (poprzednio 3.5) — warstwy i trade-offy trafne, nie wiedział o `?fields=` query param + @Expose groups. Explain-concept przeprowadzony, quiz zaliczony. Interval 5d → 26d.

Lapse: brak. Nowa słabość: GraphQL jako alternatywa — nadal nie pojawia się samodzielnie.

## Briefing utrwalenie check

N/A — brak briefingu dzisiaj.

## Co poszło dobrze

- Decyzja architektoniczna: opaque UUID → custom guard zamiast Passport — samodzielna konkluzja
- TOCTOU fix: po 1 pytaniu trafne rozwiązanie (updateMany count) — nie musiałem tłumaczyć mechanizmu
- Prisma → SQL mapping: 4.5/5 — samodzielnie, bez podpowiedzi
- include: { user: { select: ... } } — proaktywnie poprawiony po uwadze

## Co poszło źle

- `@Public()` na `/refresh/token` pominięty — fundamentalny błąd w rozumieniu guard pipeline (JwtAuthGuard wykona się przed RefreshTokenGuard)
- REST sparse fields: nie wiedział o `?fields=` + @Expose groups — explain-concept wymagany
- GraphQL jako alternatywa — nadal nie pojawia się samodzielnie

## Słabości — update

- **Cache jako pierwsza warstwa** — NADAL (z poprzednich sesji)
- **@Public() na nowych publicznych endpointach** — NOWA. Jakub nie pomyślał że global guard odrzuci /refresh/token bez tego. Może być pattern do wdrożenia w następnych endpointach.
- **GraphQL jako alternatywa** — NADAL

## Faza coachingu

**Faza 1** (M4 aktywny). Samodzielność **4** — progres stabilny (4 sesje z rzędu 3.5-4). Przejście do Fazy 2 przy 4 sesjach z 4-5.

## Następna sesja

### Rekomendacja głównego taska

Pozostałe M4 core checkpointy:
- **Ownership guard** — custom guard sprawdzający `userId === resource.userId` dla PATCH/DELETE na workout-templates/workout-logs. Logicznie następny po RefreshTokenGuard (ten sam pattern, inne zastosowanie).
- **Security basics** — CORS (konfig per env), helmet, rate limiting global + per /auth. Alternatywa jeśli guard jest za mały task.
- **Unit testy AuthService** — min. 2 testy (validateUser, hashPassword). Zależy od czasu.

Rekomendacja: **Ownership guard** jako main task + **rate limiting** jako second task jeśli zostanie czas.

### Task briefing — relevant score-0 topics

Brak score-0 topics w M4 (wszystkie briefed). Dla ownership guard nie ma score-0 relevantu — temat to aplikacja guard patternu który Jakub już zna.

### Obserwacje z sesji

- **Słabości do zaadresowania:** @Public() pattern na nowych publicznych endpointach — świadomość global guard pipeline
- **Poprawki z review:** drobiazgi w session logu — nie jako osobna rozgrzewka, przy ownership guard wyjdą naturalnie w review
- **Co poszło dobrze:** TOCTOU samodzielnie, custom guard decision, Prisma SQL mapping 4.5/5

### Materiały do nauki przed sesją

Brak — ownership guard to aplikacja patternu który Jakub rozumie.

## Notatki na bieżąco
