# Sesja 2026-03-20

## Co robił

Podłączenie Prismy + PostgreSQL do projektu. Zamiana in-memory CRUD na prawdziwą bazę danych. Dodanie walidacji (class-validator, ValidationPipe, PartialType, ParseUUIDPipe).

## Samodzielność (1-5)

3.5 — Pisał cały kod sam. Debugował problemy z dockerem (port mapping, volume, env vars) z minimalnym naprowadzaniem. Pytał dobre pytania (PartialType, duplikacja, ParseUUIDPipe). Nadal potrzebuje naprowadzania na backendowe koncepty (dlaczego .env się nie ładuje, jak działa ValidationPipe).

## Co poszło dobrze

- Sam napisał cały serwis na Prisma — findMany, create, findUnique, update, delete
- Sam zauważył problemy z walidacją: duplikacja dekoratorów, brak walidacji UUID na params, słabe error messages
- Dobrze odpowiedział na pytania z roadmapy (service vs controller, DTO, kody HTTP)
- Szybko ogarnął PartialType i ParseUUIDPipe po wskazaniu kierunku
- Sam debugował problemy z dockerem (port 5437:5437 vs 5437:5432, stary volume z innym userem)

## Co poszło źle

- Nie przeczytał pytań z roadmapy przed sesją (zadanie z poprzedniej sesji)
- Prosił o ghostwriting ("wez mi zainstaluj prisme globalnie")
- Nie wiedział że Node.js nie ładuje .env automatycznie — myślenie "Next.js magicznie to robi"
- Chciał czytać o custom pipes i "wszystkich konceptach Nesta" zamiast skupić się na zadaniu — trzeba było trzymać focus

## Słabości — update

- **Myślenie ekranami** — nie pojawiło się dziś, sesja była czysto backendowa
- **Over-engineering** — chciał od razu robić response DTO i mappowanie 1:1 "na przyszłość". Skorygowane.
- **Next.js magic assumptions** — nowa obserwacja. Zakłada że rzeczy działają automagicznie (env vars, global scope). Wymaga tłumaczenia co Next.js robi za niego.
- **Focus** — chce czytać o wszystkim naraz zamiast implementować. Trzeba trzymać w ryzach.

## Faza coachingu

Faza 1 (tydzień 1-3). Progres widoczny vs poprzednia sesja — mniej naprowadzania, sam debuguje. Samodzielność rośnie. Za wcześnie na fazę 2.

## Następna sesja

- Error handling: spójne odpowiedzi HTTP (NotFoundException, BadRequestException, obsługa błędów Prisma)
- Przeczytać https://docs.nestjs.com/exception-filters PRZED sesją
- Rozważyć podejście: try/catch w serwisie vs Exception Filter — przyjść z opinią
