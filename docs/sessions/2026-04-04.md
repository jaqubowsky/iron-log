# Sesja 2026-04-04

## Plan sesji
- **Czas:** 1h
- **Milestone:** M4 (🔴 BLOKUJE — Auth + Security)
- **Rozgrzewka:** brak (PrismaMapper unification — niski priorytet, nie bloker)
- **Main task:** Rozpoczęcie M4 — JWT auth (sprawdzić homework, potem konfiguracja AuthModule + register endpoint)
- **Temat poboczny:** brak
- **Docs:** JWT.io, Auth0 access vs refresh tokens, NestJS Authentication

## Recall challenge

Pytanie: "cursor vs offset pagination — różnice w implementacji, trade-offy?" (mock interview 2026-03-25, 3/5, 7 sesji temu). Score: 4/5 — performance, cursor bez totalCount (limit+1), duplicate createdAt edge case (rzadko kto go wymienia). Po naprowadzeniu scenariuszem zrozumiał real-time consistency issue (offset = duplikaty/pominięcia gdy inserty między stronami). Progres vs 3/5 z marca.

## Co robił

Pierwsze kroki M4 — Prisma schema (User + RefreshToken), migracja, UserModule (UserRepository abstract + PrismaUsersRepository, UsersService, DI binding, CreateUserDTO), AuthModule scaffold (pusty — kontroler, serwis, moduł bez logiki). Register endpoint na następną sesję.

## Planowanie architektoniczne

Solidny plan strategiczny przed kodowaniem: UserModule osobno od AuthModule (separacja odpowiedzialności), repository pattern dla User, refresh + access token flow. Wiedział że trzeba zacząć od schematu. Nie wymienił konkretnej kolejności plików ale task był dobrze rozumiany architekturonicznie.

## Code review

- **Sam zauważył:** `include: { tokens: true }` jest niepotrzebne w findByEmail/create — zostawił na później ("zobaczę potem")
- **Pominął:** DI binding dla `UsersRepository` w `UsersModule` (po promptie naprawił od razu), `onDelete: Cascade` na RefreshToken (po pytaniu naprawił), `@MinLength(8)` na password (po skan rezultacie — naprawił)
- **Po naprowadzeniu:** wszystkie bugi naprawione w trakcie sesji

## Samodzielność (1-5)

3 — Schema i UserModule zrobione samodzielnie, plan architektoniczny poprawny. Trzy rzeczy pominięte bez pomocy: DI binding, onDelete na RefreshToken, MinLength. Każdą naprawił od razu po naprowadzeniu. Nie próbował ghostwritingu.

## Explain phase

3.5/5 — Token rotation detection (valid flag → revoke all) opisany poprawnie i samodzielnie. DI binding: "żeby podmienić implementację (Mongo, testy)" — trafne. Separacja service/repo eksportu: "nie couplujesz repo z innym modułem" — poprawne. Brakowało: decyzja `userId required` na WorkoutLog (konsekwencje dla ownership check), onDelete Cascade reasoning.

## Mock interview

Pytanie: "POST /workout-logs przetwarza 50MB CSV, 30 sek, blokuje serwer — co jest przyczyną i jak naprawisz?" Score: 3/5. Trafnie: ładowanie do pamięci, readable streams, chunking. Po pytaniu: single-threaded event loop — poprawnie wyjaśnił. Brakowało: queue (BullMQ) jako async offloading, user dostaje 202 Accepted nie czeka. Pierwsze zetknięcie z event loop tematyką (M5).

## Co poszło dobrze

- Homework z JWT zrobiony — przyszedł z solidnym mental modelem access+refresh token flow
- Token rotation detection (valid flag + reuse detection) — wytłumaczył bez naprowadzania
- Schema zaprojektowana samodzielnie i poprawnie — RefreshToken z valid Boolean, relacje, kaskady
- DI binding poprawiony od razu po pytaniu sokratejskim (nie po bezpośredniej odpowiedzi)
- Cursor pagination recall: 4/5 — progres i zapamiętał edge case (duplicate createdAt)

## Co poszło źle

- Trzy rzeczy w kodzie pominięte bez promptu: DI binding, onDelete na RefreshToken, MinLength na password — każda istotna (DI = runtime crash, onDelete = data integrity, MinLength = security)
- Explain: nie wspomniał o decyzji `userId required` na WorkoutLog mimo że to istotna decyzja M4

## Słabości — update

- **Self-review nawyk** — NOWA obserwacja. Jakub nie robi self-review przed oddaniem kodu. Trzy issues w tym samym patternie: DI binding, onDelete, MinLength — każde widoczne przy uważnym przejrzeniu własnego kodu. Potrzebuje nawyku "przejdź przez każdy plik i zapytaj: czy to może nie działać?"
- **Explain — motywacja decyzji** — NADAL. Wytłumaczył CO zrobił ale pominął dlaczego konkretne decyzje (userId required, Cascade) były ważne architektonicznie

## Faza coachingu

Faza 1 (M1-M3 zakończone, teraz M4). Samodzielność 3 — stabilna. Przejście do Fazy 2 nie jeszcze (potrzeba 4+ sesji z 4-5 samodzielności).

## Następna sesja

### Rekomendacja głównego taska
**UWAGA: backlog retencji wynosi ~14 nieodhaczonych "potrafię wytłumaczyć" checkpointów z M2 i M3.** Protokół mówi: >5 → dedykowana sesja `/mock-interview session`. Session-start powinien rozważyć sesję artykulacyjną (5-6 pytań z backlogu M2/M3) zamiast kodowania. Jeśli Jakub ma dużo czasu (2h+) — można split: 30 min mock session + 1h register endpoint. Alternatywa kodowa: M4 kontynuacja — `POST /auth/register` end-to-end (AuthService bcrypt + duplikat email 409 + UsersModule import).

### Obserwacje z sesji (input dla session-start)
- **Słabości do zaadresowania:** Self-review nawyk (3 issues pominięte bez promptu — DI binding, onDelete, MinLength). Przed oddaniem kodu: "sprawdź każdy plik czy może nie działać w runtime"
- **Poprawki z review:** `include: { tokens: true }` w findByEmail/create — do usunięcia przy implementacji login (nie bloker teraz)
- **Co poszło dobrze:** JWT homework solidny, token rotation detection bez naprowadzania, schema samodzielna

### Retencja (tematy do weryfikacji)
- JWT access + refresh token flow + token theft (pierwsze omówienie 2026-04-04, ⏳ 3.5/5)
- Event loop + single-threaded (pierwsze zetknięcie 2026-04-04 przez mock interview, ⏳ 3/5)

---

## Sesja 2026-04-04 (kontynuacja — auth service)

## Notatki na bieżąco
- recall: onDelete Cascade — "usuwa powiązane rekordy... chyba?" — 2/5 (2 hinty)
- słabość: onDelete Cascade — nie wiedział jakie typy kaskady istnieją, nie wymienił RESTRICT/SET NULL jako alternatyw
- ghostwriting attempt: hashowanie hasła — "pokaż mi jak"
- dobry moment: findByEmail z UsersService — sam doszedł bez podpowiedzi ("nie myślałem żeby użyć serwisu")
- słabość: JWT payload — potrzebował 2 pytań na tym samym konceptcie (claims vs dane użytkownika)

### Materiały do nauki przed sesją
- **Docs:** NestJS Security → Hashing (bcrypt), NestJS Exception Filters (duplikat email → 409)
- **Pytanie do przemyślenia:** Register hashuje hasło, potem zapisuje do bazy. Co się stanie jeśli `findByEmail` znajdzie duplikat PRZED hashem? A co jeśli sprawdzasz AFTER zapisujesz? Gdzie powinien być ten check?
